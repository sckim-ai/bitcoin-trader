# History

| 날짜 | 시간 | 결과 | 요약 |
|------|------|------|------|
| 2026-04-30 | - | success | 거래 알림 4종(+late+wait) 메시지 개선 + 6종 일괄 테스트. 사용자 요구. **개선** ([notifications/manager.rs](src-tauri/src/notifications/manager.rs)): (1) `notify_ready`에 `session_label` 인자 추가, 매수=🔵 / 매도=🟠 별 emoji + 두 줄 형식 (`↳ session: <label>`). (2) `notify_order_registered(market, side, target_price, session_label)` 신규 — limit wait 등록 전용 (📥 매수 / 📤 매도). 기존에 `notify_signal`로 보내던 wait 메시지를 의미 명확한 메서드로 분리. (3) `notify_trade_full(side, market, price, volume, pnl_pct, note, late)` 신규 — `late=true`면 `⏱🟢`/`⏱🔴`로 cycle 동기 fill과 시각 구분. KST 타임스탬프 자동 추가(`⏰ 2026-04-30 11:00:23 KST`). 기존 `notify_trade_rich`는 wrapper 유지(late=false). **호출 사이트**: [session_engine.rs](src-tauri/src/services/session_engine.rs) — `notify_ready`에 `Some(&session.label)`, wait 등록은 `notify_order_registered`, [pending_order_tracker.rs](src-tauri/src/services/pending_order_tracker.rs) late fill은 `notify_trade_full(..., late=true)`. **테스트 인프라** ([commands/notification.rs](src-tauri/src/commands/notification.rs)): `test_trade_notifications` 커맨드 — 6 변형(매수대기/매도대기/매수/매도/주문등록/늦은체결)을 enabled 채널에 800ms 간격 순차 전송 + 결과 메시지 반환. NotificationManager.from_db로 enabled 무시 안 함(런타임 경로 검증). [SettingsPage.tsx](src/pages/SettingsPage.tsx) Notifications 카드 하단에 `[Send 6 samples]` 섹션 + 결과 표시(✓/✗ 색 분리). [lib/api.ts](src/lib/api.ts) `testTradeNotifications` API. **검증**: `cargo build --features tauri-app` 통과, `npx tsc -b` exit 0, `npm run vite:build` 통과. 사용자가 Settings → Send 6 samples 클릭 시 Discord 채널에 6 메시지 5초 내 도착 예상. |
| 2026-04-30 | - | success | 수동 주문에 시장가/지정가 선택 추가 — `ManualOrderArgs.ord_type` + 4조합 분기. 사용자 요구. **백엔드** ([commands/trading.rs](src-tauri/src/commands/trading.rs)): `ManualOrderArgs`에 `ord_type` ("market"|"limit", default "market") + `limit_price: Option<f64>` 추가. `(side, ord_type)` 4조합 — market+buy → `place_market_buy`, market+sell → `place_market_sell`, limit+buy → `place_limit_buy_typed`(volume = `floor(krw/price×1e8)/1e8` 자동 환산 또는 사용자 직접 volume), limit+sell → `place_limit_sell_typed`. limit+buy 명목금액 5K 미만 거부. 응답 처리: limit 주문이 wait이면 live_trades insert **건너뜀** (Upbit 호가창에 등록되어 사용자가 직접 추적, manual은 cycle cancel 정책에서 분리). limit done이면 `limit_price`를 `booked_price`로 사용. signal: `manual_buy_limit`/`manual_sell_limit` 추가. **프런트**: [lib/api.ts](src/lib/api.ts) `ManualOrderArgs`에 `ord_type`/`limit_price` 추가. [ManualOrderCard.tsx](src/components/live/ManualOrderCard.tsx) — Buy/Sell 토글 옆에 Market/Limit 토글, limit 모드면 "Limit Price (KRW)" 입력 필드 표시. limit+buy 시 KRW + price → 예상 수량(8자리) 즉시 미리보기. confirm 다이얼로그에 시장가/지정가 라벨, (limit 시) 가격 + 예상 수량 + 안내 문구. 버튼 라벨 `Buy (market)` / `Buy (limit)` 등으로 명시. [HistoryPage.tsx](src/pages/HistoryPage.tsx) `formatSignal`에 `manual_*_limit → "(manual, limit)"` 매핑. **검증**: `cargo build --features tauri-app` 통과, `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-30 | - | success | confirm 다이얼로그 일괄 마이그레이션 — 잔여 7곳 `window.confirm()` → `confirmDialog()` 교체 완료. 코드베이스 전체 `window.confirm` / 베어 `confirm(` 잔존 0건 확인. **위험도 매핑**: danger(rose, 되돌릴 수 없는 작업) — Kill switch ([LiveTradingPage.tsx](src/pages/LiveTradingPage.tsx) handleKillSwitch), 세션 삭제(SessionTable onDelete), 사용자 삭제([AdminPage.tsx](src/pages/AdminPage.tsx) handleDelete), Run 삭제([OptimizationPage.tsx](src/pages/OptimizationPage.tsx) handleDeleteRun). warning(amber, 재생성/재입력 가능) — Upbit 키 삭제([SettingsPage.tsx](src/pages/SettingsPage.tsx) handleClearKeys), Paper 환원(handleDemote), Preset 삭제. **JSX body 활용**: 사용자명/preset 이름 amber+font-data 강조, 세션 ID 는 [Badge](src/components/ui/Badge.tsx) variant="amber", Optimization Run 은 `#id strategy_key pop×gen` 구조화 표시, 부가 경고는 `text-xs text-zinc-500` 보조 라인. 인라인 `onClick={() => confirm() && fn()}` 두 곳은 `onClick={async () => {...}}` 로 변환 — TS 가 `() => void` 콜백 위치에 `Promise<void>` 화살표 함수 허용. **ConfirmDialog.tsx 보강**: 컴포넌트(Host) + 함수(confirmDialog) 동시 export 로 인한 react-refresh 경고는 함수 위에 `eslint-disable-next-line react-refresh/only-export-components` 1줄 — 모듈 레벨 listener/pending state 가 두 export 간 공유되어야 해 분리 시 별도 store 파일이 강제되는 트레이드오프 회피 (Simple is Best). **검증**: `npm run vite:build` 통과 (2378 modules, +2.24KB ungzipped / +0.35KB gzipped 번들), 수정 6파일 ESLint 0 errors. ManualOrderCard 의 unused vars(ordType/limitPrice/estimatedVolume) 는 limit-order WIP 잔재로 이번 변경 무관 — 별도 정리 필요. |
| 2026-04-30 | - | success | 수동 주문 confirm 다이얼로그 — 브라우저 `window.confirm` ("localhost:1420의 메시지" 라벨 노출) 을 앱 디자인 시스템에 맞춘 커스텀 모달로 교체. **신규** [src/components/ui/ConfirmDialog.tsx](src/components/ui/ConfirmDialog.tsx): Promise 기반 명령형 API `confirmDialog({title, body, severity, confirmLabel, cancelLabel})` + `<ConfirmDialogHost />`. 모듈 레벨 listener + pending 변수로 React 외부에서 단일 Host 의 setState 트리거 — 매 호출마다 createRoot 안 띄워 store/router 컨텍스트 보존. `severity: info \| warning \| danger` 별 아이콘(Info/AlertTriangle) + 색상(sky/amber/rose) + 확인 버튼 variant 매핑. Esc=취소, Enter=확인, 백드롭 클릭=취소, autoFocus 확인. body 가 `ReactNode` 라 Badge/금액 강조/세션 라벨 등 리치 콘텐츠 가능. **마운트** [src/App.tsx](src/App.tsx): 로그인 화면·메인 트리 양쪽에 `<ConfirmDialogHost />` 1회 마운트(BrowserRouter 안). **교체** [src/components/live/ManualOrderCard.tsx](src/components/live/ManualOrderCard.tsx): 100만 초과 매수 confirm → severity="warning", 실주문 confirm → severity="danger" + side 별 confirmLabel + 금액/ETH/시장가 컬러 강조 + Badge 로 세션 표시 + 슬리피지 안내. 기존 `desc`/`sessionLabel` 문자열 조립 제거. **검증**: `npm run vite:build` 통과(2378 modules), 새 파일 lint clean. 다른 8곳 confirm() 호출(LiveTradingPage 4곳, SettingsPage/AdminPage/OptimizationPage)은 추후 사용자 요청 시 일괄 교체. |
| 2026-04-30 | - | success | Upbit JWT 401 invalid_query_payload 수정 — query_hash 알파벳순 정렬 누락. **증상**: 수동 매수 시 `Upbit 401 Unauthorized — Failed to verify the query of Jwt`. **원인**: Upbit JWT 인증은 `query_hash`를 SHA512(정렬된 query string)로 계산. 본 코드의 `place_market_buy`/`place_market_sell`/`place_limit_typed`/`place_order` 4개 메서드 모두 `format!("market=...&side=...&...")` 식으로 임의 순서 query를 만들어 hash. Upbit가 자기 정렬과 비교 시 mismatch → 401. 수동 주문이 실 호출의 첫 케이스라 발견됨 (자동매매는 실거래까지 도달한 적 없음). **수정**: [api/upbit.rs](src-tauri/src/api/upbit.rs)에 `build_sorted_query(pairs)` 헬퍼 — `(key, value)` Vec을 key 알파벳순으로 sort 후 `key=value&...` 결합. 4개 주문 메서드 모두 헬퍼로 일괄 변환. **검증**: `cargo build --features tauri-app` 통과, `cargo test --test cum_return_parity_test` 5/5. 즉시 가능: 사용자가 수동 매수 재시도. |
| 2026-04-30 | - | success | 수동 매수/매도 UI — 자동매매 흐름 외부의 사용자 트리거 시장가 주문. 첫 실거래 검증 도구. **백엔드** ([commands/trading.rs](src-tauri/src/commands/trading.rs)): `manual_market_order(args)` Tauri command — `ManualOrderArgs { market, side, krw_amount?, volume?, session_id? }`. side="buy" → `place_market_buy(market, krw)`, side="sell" → `place_market_sell(market, vol)`. 5K KRW 최소 가드. session_id 제공 시 live_trades에 is_real=1 + signal=`manual_buy`/`manual_sell` row 삽입(자동매매 `real_buy`와 명확히 구분). sell pnl/pnl_pct는 session.current_buy_price 기준 best-effort. **프런트**: [lib/api.ts](src/lib/api.ts)에 `manualMarketOrder` + 타입. [ManualOrderCard.tsx](src/components/live/ManualOrderCard.tsx) 신규 — Buy/Sell 토글, KRW(buy)/ETH(sell) 입력 비대칭, 다중 가드(최소 5K, 100만 초과 confirm, 빈 값/0 거부), 실행 전 confirm 다이얼로그 ("⚠️ 실제 자금 이동" + 부착 세션 표시), 응답 후 UUID/State/Executed 표시(state=wait면 "다음 cycle에서 reconcile" 안내). multi-real=1 정책상 real 세션 0개면 카드는 사용 가능하나 "fill won't attribute to history" 경고. [LiveTradingPage.tsx](src/pages/LiveTradingPage.tsx) PendingOrdersWidget 아래에 통합. [HistoryPage.tsx](src/pages/HistoryPage.tsx) formatSignal에 `manual_buy → buy (manual)` / `manual_sell → sell (manual)` 매핑 추가. **검증**: `cargo build --features tauri-app` 통과, `npx tsc -b` exit 0, `npm run vite:build` 통과. 실주문 실행은 사용자 직접. |
| 2026-04-30 | - | success | 히스토리 페이지 7가지 보강 (A~G). 실거래 발생 전 미리 보강해 첫 운용에서 즉시 신뢰 구축. **A. 자동 갱신** ([HistoryPage.tsx](src/pages/HistoryPage.tsx)): SessionTable과 같은 `session:update` Tauri 이벤트 구독 → real cycle 종료 시 silent reload. 수동 Reload burden 제거. **B. KST 시간 표시**: `formatKst(ts)` 헬퍼 — `toLocaleString("ko-KR", {timeZone: "Asia/Seoul"})`. 표 컬럼 헤더도 "Time (KST)"로. 차트는 "UTC 일자 기준" 명시(GROUP BY가 UTC라 일관성 유지). 페이지 헤더에 "시간대: KST" 표시. **C. 세션 라벨 매핑**: `listSessions` 호출 → `id → label` Map. 표에서 `#1 Short_156%` 형태로 표시. 삭제된 세션은 `(deleted)` 회색 처리. **D. CSV UTF-8 BOM** ([history.rs](src-tauri/src/commands/history.rs)): 출력 앞에 `\u{FEFF}` 추가 — 한국어 Excel에서 ts/한글 깨짐 방지. **E. signal 라벨 정리**: `formatSignal` 헬퍼 — `real_buy → buy`, `real_buy_late → buy (late)` 등. CSV는 backend 원본 유지(분석용). **F. 세션 셀렉터**: 필터 바에 dropdown(`All` + 각 세션 `#N label ★(real)`). HistoryFilter.session_id로 backend에 전달. **G. fee 컬럼**: 표에 Fee 컬럼 추가 + KPI에 "Total fee (KRW)" 5번째 카드. **검증**: `cargo build --features tauri-app` 통과, `npx tsc -b` exit 0, `npm run vite:build` 통과. is_real=1 trades 0건 상태(첫 운용 전)이라 화면 검증은 첫 실거래 후. |
| 2026-04-30 | - | success | 알림 테스트 silent 실패 수정 — Test 버튼이 enabled 무시 + 정확한 에러 노출. **증상**: "Save 성공 토스트 + Test 'Sent!' 토스트가 떴는데 Discord 메시지 안 옴". **DB 확인**: `notification_configs.enabled = 0` 으로 저장됨 (사용자가 토글 미체크). 옛 흐름은 NotificationManager.from_db가 `enabled = 0`인 채널의 client 자체를 안 만들어 `notify_alert`이 silent 통과 — Test 토스트는 무조건 "Sent!". 추가로 DiscordClient.send도 응답 status 검증 없이 통과해 5xx/404도 silent. **백엔드** ([commands/notification.rs](src-tauri/src/commands/notification.rs)): `test_notification`을 NotificationManager 우회로 재작성 — DB에서 config row 직접 조회 (enabled 무시), 채널별로 직접 client 생성 + send + 결과 문자열 반환. 빈 webhook URL/token도 명시적 에러. [discord.rs](src-tauri/src/notifications/discord.rs): `send`가 `status.is_success()` 검증 + 본문을 에러에 포함. **프런트** ([SettingsPage.tsx](src/pages/SettingsPage.tsx)): Save 시 enabled=false면 "Saved (disabled — 알림 안 감)" amber 경고. Test 시 결과를 `✓`/`✗` 접두사 + 색(emerald/rose) 분리 + 에러는 자동 안 사라짐(사용자가 복사 가능). **검증**: `cargo build --features tauri-app` 통과, `npm run vite:build` 통과. 즉시 해결: 사용자가 Discord 토글을 켜고 다시 Save하거나, 새 Test 버튼은 토글 무관 동작. |
| 2026-04-30 | - | success | 알림 설정 "Not authenticated" 에러 수정 — single-user 모델 일관성. **증상**: Settings에서 알림 설정 저장 시 `Error: Not authenticated`. **원인**: `save_notification_config`/`test_notification`만 session token 검증을 요구. 다른 모든 commands(insert_session/list_presets/save_preset/save_upbit_keys 등)는 user_id=1(default admin) 하드코딩. notification만 multi-user 잔재로 token 의존이 남아있어, 24h 만료 또는 미로그인 시 발동. **수정**: [commands/notification.rs](src-tauri/src/commands/notification.rs)에서 token 인자 제거 + `DEFAULT_USER_ID = 1` 하드코딩 사용. `auth::session` 의존성 제거. [lib/api.ts](src/lib/api.ts)의 saveNotificationConfig/testNotification 시그니처에서 token 파라미터 제거. [SettingsPage.tsx](src/pages/SettingsPage.tsx) 호출도 정리. **검증**: `cargo build --features tauri-app` 통과, `npx tsc -b` exit 0, `npm run vite:build` 통과. 보안 표면: notification 채널 자격증명(Discord webhook 등)은 여전히 DB의 notification_configs에 보호되어 OS 사용자 = 앱 사용자 가정. |
| 2026-04-30 | - | success | 미체결 주문 위젯 — LiveTradingPage 상단 collapse 카드. **목적**: 정상 운용에선 0행이지만 Upbit 장애 / 가격 갭으로 wait가 누적될 때 사용자가 즉시 식별. **백엔드**: [commands/live_trading.rs](src-tauri/src/commands/live_trading.rs)에 `list_pending_orders` 커맨드 + `PendingOrderRow` DTO. live_repo의 list_pending_wait 결과를 frontend 친화 형태로 변환. **프런트**: [lib/live.ts](src/lib/live.ts)에 `listPendingOrders` + `PendingOrderRow` 타입. [PendingOrdersWidget.tsx](src/components/live/PendingOrdersWidget.tsx) 신규 — 30초 폴링, 비어있으면 카드 자체를 숨김(steady state에 노이즈 없음), 헤더 클릭으로 확장/접힘. 표 컬럼: UUID(8자) / Session / Side(buy/sell 색) / Target price / Requested(KRW or volume) / Placed(시각) / Last check. [LiveTradingPage.tsx](src/pages/LiveTradingPage.tsx)에 SessionTable 위에 배치. **검증**: `cargo build --features tauri-app` 통과, `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-30 | - | success | 로그 파일 보존 — 일별 회전 파일 + live_log! 매크로. **목적**: real cycle의 진단 로그(stderr `[real cycle]/[real BUY/SELL]/[pending_tracker]/[order_executor]`)가 콘솔 닫히면 사라지던 문제. 사후 분석용으로 영구 파일 저장. **신규 모듈**: [core/file_logger.rs](src-tauri/src/core/file_logger.rs) — `OnceLock<PathBuf>`로 logs 디렉토리 1회 결정 (`%LOCALAPPDATA%/bitcoin-trader/logs/`), 매 호출마다 `live_<YYYY-MM-DD>.log` 빌드 → append. 자정 넘어가면 자동 새 파일. write 실패는 silent (로그가 트레이드 루프 막지 않게). **매크로**: `live_log!("[real ...]", args...)` — eprintln + 파일 append 동시. 기존 stderr 흐름은 유지(콘솔 실시간 모니터링 보존). **교체**: session_engine `[real cycle]`/`[real BUY]`/`[real SELL]` 11곳, pending_order_tracker `[pending_tracker]` 6곳, order_executor `[order_executor]` 2곳 일괄 교체. 그 외 디버그성 eprintln(에러 처리, 비-cycle)은 유지. **검증**: `cargo build --features tauri-app` 통과, `cargo test --test cum_return_parity_test` 5/5. 첫 real cycle 후 `%LOCALAPPDATA%/bitcoin-trader/logs/live_2026-04-30.log` 파일 생성 + 사이클별 줄 추가 확인 가능. |
| 2026-04-30 | - | success | Phase 4C — Trading History 페이지 (real trades 누적 + 일별 P/L 차트 + CSV export). **백엔드** ([commands/history.rs](src-tauri/src/commands/history.rs) 신규): `list_real_trades(filter)` (session/since/until 필터 + LIMIT 5000), `real_pnl_summary(filter)` (UTC 일자별 GROUP BY → DailyBucket {date, count, pnl_sum, avg_pct}), `export_real_trades_csv(filter)` (CSV 문자열 반환). 모두 `is_real=1` 한정. 동적 SQL은 `params_from_iter`로 안전 바인딩. **프런트**: [lib/api.ts](src/lib/api.ts)에 3 함수 + 타입(HistoryFilter/HistoryTrade/DailyBucket). [pages/HistoryPage.tsx](src/pages/HistoryPage.tsx) 신규 — Since/Until 입력, 4 KPI 카드(Realized P/L/Sells/Profit days/Loss days), SVG 일별 막대차트(파/빨, 5일마다 라벨 회전), 거래 테이블(시간/세션/side/price/volume/pnl/pnl%/signal). CSV 다운로드는 Blob+a.click. [App.tsx](src/App.tsx)에 `/history` 라우트 + 네비 아이콘 추가. **검증**: `cargo build --features tauri-app` 통과, `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-30 | - | success | Live 알림 4종 연결 — 매수/매도 + 매수대기/매도대기 통보. 사용자 요구. **NotificationManager 확장** ([manager.rs](src-tauri/src/notifications/manager.rs)): `notify_ready(market, side, target_price)` (📊 매수/매도 대기, close ≈ N원), `notify_trade_rich(side, market, price, volume, pnl_pct, note)` (🟢/🔴 + 다중 라인 + 부가 컨텍스트). 기존 `notify_trade` 그대로 유지. **session_engine real 분기** ([session_engine.rs](src-tauri/src/services/session_engine.rs)): cycle 시작 시 NotificationManager.from_db 1회 build (락 안에서) → `prev_signal = session.last_signal` 기억 → 매수/매도 즉시 체결 시 `notify_trade_rich`, wait만 발생 시 `notify_signal`(주문 등록), "buy ready"/"sell ready" 신호 + 직전 last_signal 다른 경우만 `notify_ready` (레거시 `_lastNotifiedSignal` 패턴, 중복 방지). DB lock을 sub-scope `{}`로 감싸 `.await` 경계 위반 방지(MutexGuard !Send). **pending_order_tracker** ([tracker](src-tauri/src/services/pending_order_tracker.rs)): done 분기에 late buy/sell 알림 추가, "late fill (placed ...)" 노트 포함. **검증**: `cargo build --features tauri-app` 통과, `cargo build --tests` 전체 통과, `cargo test --test cum_return_parity_test` 5/5. |
| 2026-04-30 | - | success | Post-4A.7 정책 변경 — 한도 무력화 + 시장가→지정가 + 다음 봉 close 재주문. 사용자 요구. **A. 한도 무력화**: [session_engine.rs](src-tauri/src/services/session_engine.rs)의 0a 회로차단기 블록 주석 처리. 가드 함수(`today_realized_pnl_pct`/`today_real_trades_count`)와 마이그레이션 011 컬럼은 향후 재활성화용 보존. SessionTable의 한도 텍스트는 'limits off'(italic)으로 변경. 회귀 가드 8 케이스는 함수 자체 테스트라 그대로 통과. **B. 지정가 모드**: [api/upbit.rs](src-tauri/src/api/upbit.rs)에 `place_limit_buy_typed`/`place_limit_sell_typed` 추가 — `OrderResponse` 반환 + `Result<_, String>` (Send-safe). [order_executor.rs](src-tauri/src/services/order_executor.rs)의 `execute_split_buy`/`sell` 시그니처에 `target_price: f64` 추가 — `> 0`이면 limit, `0`이면 기존 market. 매수는 `volume = floor(KRW/target × 1e8) / 1e8`. session_engine은 `target_price = confirmed.last().candle.close` 전달 (= 신호 발생 봉의 close). 응답 처리: `is_done()` chunk만 `live_trades` insert + position 갱신 — `wait` chunk는 `pending_orders`만 들어가고 position은 다음 cycle reconcile에 위임. **C. 다음 봉 close 재주문**: [live_repo](src-tauri/src/db/live_repo.rs)에 `list_session_pending_wait(session_id)` 추가. session_engine real cycle 진입 시 `cancel_session_wait_orders` 헬퍼로 같은 세션 wait 주문 모두 Upbit cancel + DB mark. [pending_order_tracker.rs](src-tauri/src/services/pending_order_tracker.rs)의 done 분기 — `executed_volume > 0`이면 `live_trades`에 `real_buy_late`/`real_sell_late` row 추가 + session position 갱신 (지정가 늦은 체결 처리). signal 명을 다르게 해 같은 cycle synchronous 경로(`real_buy`)와 충돌 방지. **검증**: `cargo build --features tauri-app` + `cargo build --tests` 전체 통과, `cargo test --test cum_return_parity_test` 5/5, `npx tsc -b` exit 0, `npm run vite:build` 통과. 마스터플랜에 변경 사항 + 위험 프로필 명시. |
| 2026-04-29 | - | success | Phase 4A.7 — 자동 회귀 테스트 + E2E 수동 검증 가이드. **추가 자동 테스트** (DB-layer guards): (1) [tests/safety_circuit_breaker_test.rs](src-tauri/tests/safety_circuit_breaker_test.rs) 8 케이스 — `today_realized_pnl_pct`/`today_real_trades_count`의 SQL 집계가 session_engine 회로차단기와 일치함을 보장. paper 제외, 어제 trades 제외, threshold 비교 정확성 등. (2) [tests/multi_real_test.rs](src-tauri/tests/multi_real_test.rs) 8 케이스 — multi-real=1 invariant (count_real_sessions의 self-exclusion 패턴), demote 슬롯 해제, **stopped real도 슬롯 점유** (잔고 충돌 방지), Kill switch가 running real만 stop + mode 보존, 토글이 다른 필드 영향 없음. **신규 매뉴얼**: [Manual/real-trading-e2e-checklist.md](Manual/real-trading-e2e-checklist.md) — 9개 섹션(키 보안/Mode 토글/Kill switch/신호 가드/주문 실행/회로차단기 손실/회로차단기 횟수/외부 잔고 reconcile/데이터 일관성)의 단계별 단계별 검증. 인위적 SQL 손실/카운트 트리거 + 원복 가이드 포함. **마스터플랜 갱신**: 운용 시작 조건 체크리스트와 자동 테스트 진척(45 케이스) 명시. **검증**: `cargo build --tests` 전체 통과, `cargo test --test cum_return_parity_test` 5/5, `npx tsc -b` exit 0, `npm run vite:build` 통과. **Phase 4A 코드 단계 완료** — 사용자 E2E 검증만 남음. |
| 2026-04-29 | - | success | Phase 4A.6 — 일일 손실 한도 + 매매횟수 제한 (circuit breaker). 마스터플랜의 마지막 안전장치. **목적**: 코드 한 줄 버그 또는 시장 급락이 자금을 한 번에 비울 수 있는 위험을 자동으로 끊는 회로 차단기. **추가**: (1) [migration 011](src-tauri/migrations/011_safety_limits.sql) — `live_sessions.max_daily_loss_pct REAL NOT NULL DEFAULT -10.0`, `max_daily_trades INTEGER NOT NULL DEFAULT 20`. (2) [models/live.rs](src-tauri/src/models/live.rs) + [types/index.ts](src/types/index.ts) — LiveSession에 두 필드 동기화. (3) [live_repo](src-tauri/src/db/live_repo.rs) — `today_realized_pnl_pct(session_id, initial_capital)` (UTC 자정 이후 is_real=1 sells의 pnl 합 / 초기자본 × 100), `today_real_trades_count(session_id)`. row_to_session 17/18/19번 인덱스 갱신. (4) [session_engine.rs](src-tauri/src/services/session_engine.rs) — real_reconcile_step 진입 즉시 한도 체크 → 위반 시 `set_session_status('stopped')` + early return (잔고 조회/주문 안 함). loss는 음수 비교 (`today_pnl_pct ≤ max_daily_loss_pct`), count는 양수 비교 (`≥ max_daily_trades`). (5) [SessionTable.tsx](src/components/live/SessionTable.tsx) — Mode 셀 아래에 `≤-10% / 20t` 작은 텍스트 (real만, tooltip으로 설명). (6) [PromoteRealDialog.tsx](src/components/live/PromoteRealDialog.tsx) — 세션 정보 박스에 "Daily safety limits" 행 추가 (사용자가 promote 시 한도 인지). 4개 setup_db에 011 추가. **검증**: `cargo build --features tauri-app` 통과, `cargo build --tests` 전체 통과, `cargo test --test cum_return_parity_test` 5/5, `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-29 | - | success | Phase 4A.5 — 미체결 주문 추적 + 1h timeout cancel. **이유**: Upbit 시장가도 즉시 응답이 `state="wait"`로 오고 몇 초 뒤 done으로 정착하는 케이스가 있음. limit 주문은 아예 호가 갭으로 미체결 상태로 누적 가능. **추가**: (1) [migration 010](src-tauri/migrations/010_pending_orders.sql) — `pending_orders` 테이블 (uuid PK, session_id FK CASCADE, side/market/ord_type/target_price/requested/placed_at/status/last_checked/resolved_at) + 두 인덱스. (2) [api/upbit.rs](src-tauri/src/api/upbit.rs) — `cancel_order(uuid)` 메서드 추가 (DELETE /v1/order). (3) [live_repo](src-tauri/src/db/live_repo.rs) — `PendingOrder` struct + `insert_pending_order` (UPSERT on uuid conflict) / `list_pending_wait` / `mark_pending_resolved` / `touch_pending_check`. setup_db에 009/010 누락 보강. (4) [services/pending_order_tracker.rs](src-tauri/src/services/pending_order_tracker.rs) 신규 — `reconcile_pending_orders(db, upbit)`: wait 상태 모두 조회 → `get_order(uuid)` → done이면 mark resolved, 1시간 초과면 `cancel_order` 후 cancel 처리. 개별 실패는 로그만 남기고 다음 uuid 진행. (5) [session_engine](src-tauri/src/services/session_engine.rs) — real cycle 진입 시 reconcile_pending_orders 먼저 호출 → 잔고 조회 → 주문. 주문 성공 직후 모든 chunk를 pending_orders에 insert (`is_done()`이면 'done', 아니면 'wait'). (6) [tests/pending_orders_test.rs](src-tauri/tests/pending_orders_test.rs) 신규 — 5 케이스(insert→list, mark done, touch check, upsert 충돌, CASCADE delete). **검증**: `cargo build --features tauri-app` 통과, `cargo build --tests` 전체 통과, `cargo test --test cum_return_parity_test` 5/5, `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-29 | - | success | Phase 4A.4 — 시장가 분할 주문 + 실주문 통합. ⚠️ 첫 자금이 실제로 움직이는 단계 — 4A.5(미체결 추적) + 4A.6(일일 손실 한도) 완료 전 실운용 금지. **추가**: (1) [api/upbit.rs](src-tauri/src/api/upbit.rs) — `OrderResponse` struct (uuid/side/state/executed_volume/trades_count 등 옵션 필드 + `is_done()`/`executed_volume_f64()` 헬퍼), `place_market_buy(market, krw_amount)` (ord_type="price"), `place_market_sell(market, volume)` (ord_type="market"), `get_order(uuid)` — 신규 메서드는 `Result<_, String>`로 통일해 async 경계의 Send 문제 해소. (2) [services/order_executor.rs](src-tauri/src/services/order_executor.rs) 신규 — 레거시 `ExecuteSplitBuy/SellAsync` 정확 포팅. 50만원 임계 3분할, 2초 간격, 마지막 chunk에 나머지 흡수, 8자리 소수점 매도 수량. `split_buy_chunks`/`split_sell_chunks` 순수 함수로 분리해 단위 테스트 9개. (3) [services/session_engine.rs](src-tauri/src/services/session_engine.rs) — `real_reconcile_step`이 `db: &Arc<Mutex<Connection>>` 추가로 받음. final_signal 분기: "buy" + krw>5K → `execute_split_buy(0.9995×krw)`, "sell" + 평가액>5K → `execute_split_sell(coin_balance)`, "buy ready"/"sell ready" → 알림 자리(notify는 후속), 그 외 → no-op. 체결 결과로 `live_trades(is_real=1)` 즉시 insert + session.current_buy_price/volume을 booked로 갱신. Upbit 즉시응답에 executed_volume=0이면 estimated(`order_krw / current_price × (1-fee)`)로 fallback — 4A.5의 `get_order` 폴링이 정밀화 예정. (4) [tests/order_executor_test.rs](src-tauri/tests/order_executor_test.rs) 신규 — split chunk 매트릭스 + OrderResponse JSON 파싱 (시장가 매수 wait/done, 시장가 매도 done) 총 11 케이스. **검증**: `cargo build --features tauri-app` 통과, `cargo test --test cum_return_parity_test` 5/5, `npx tsc -b` exit 0, `npm run vite:build` 통과. lib 의존 통합 테스트는 사전 환경 DLL 이슈로 실행 불가(컴파일만 검증). |
| 2026-04-29 | - | success | Phase 4A.3 — resolve_live_signal + session_engine real 분기. **목적**: 레거시 C# `ResolveSignalFromSimulation` 정확 포팅 — 시뮬상 holding이라도 실 잔고가 현금이면 매수 X, 그 반대도 마찬가지. 4A.4의 실주문 직전까지 (신호 결정 + 잔고 reconcile) 구현. **신규 모듈**: [core/live_signal.rs](src-tauri/src/core/live_signal.rs) — `last_signal_from_simulation`(signal_log 마지막 entry 추출), `resolve_live_signal(sim_signal, position)` (12 케이스 매트릭스), `filter_confirmed_candles`(미완성 마지막 봉 제외, 레거시 `FilterConfirmedCandles` 포팅). **session_engine 변경**: (1) trade write 블록을 `{ }`로 감싸 MutexGuard 수명 제한 — `.await` 직전 drop (CLAUDE.md 규칙). (2) `mode == "real"` 분기에서 `real_reconcile_step` 호출: Upbit `get_balance`/`get_current_price` → `reconcile_position` (DB↔실잔고) → `resolve_live_signal`로 final 결정 → `current_position`/`cbp`/`cbv`/`last_signal` mutate. 실주문은 4A.4. (3) update_session_cycle은 별도 `{ }` 블록에서 conn 재획득. **테스트**: [tests/live_signal_test.rs](src-tauri/tests/live_signal_test.rs) 신규 — 13 케이스(12 매트릭스 + 미지 position 방어). 단, lib 의존 통합 테스트는 사전 환경 DLL 이슈로 실행 불가 → 컴파일 통과만 확인. **검증**: `cargo build --features tauri-app` 통과, `cargo test --test cum_return_parity_test` 5/5, `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-29 | - | success | Phase 4A.2 — Mode 토글 + multi-real=1 강제 + Kill Switch. **목적**: 실거래(real) 세션은 동시에 1개만 운용 강제(잔고 충돌 방지) + 사용자가 명시적 confirm 후에만 paper→real 승격. **백엔드**: (1) [live_repo](src-tauri/src/db/live_repo.rs) — `set_session_mode`, `count_real_sessions(exclude)`, `stop_all_real_sessions()` 신규 + 단위 테스트 2개. (2) [commands/live_trading.rs](src-tauri/src/commands/live_trading.rs) — `toggle_session_mode` (paper↔real, real 승격 시 keyring 키 검사 + multi-real=1 검증) + `emergency_stop_all_real` (running real 모두 stop, mode 보존). **프런트**: (3) [lib/live.ts](src/lib/live.ts) `toggleSessionMode`/`emergencyStopAllReal` API. (4) [liveTradingStore](src/stores/liveTradingStore.ts) 액션 추가. (5) [PromoteRealDialog](src/components/live/PromoteRealDialog.tsx) 신규 — hard confirm 다이얼로그(체크박스 + “Promote to Real” 버튼, 첫 운용 50K 권장 안내, multi-real 정책 설명). (6) [SessionTable](src/components/live/SessionTable.tsx) — Mode 컬럼 추가(REAL=amber/paper=default), 행마다 `→ Real` (paper 시) / `→ Paper` (real 시) 토글 버튼. (7) [LiveTradingPage](src/pages/LiveTradingPage.tsx) — 헤더에 “REAL N” 배지 + running real ≥1일 때만 보이는 빨간 Kill switch 버튼(window.confirm + 결과 alert). 카드 제목 “Paper Sessions” → “Sessions”. **검증**: cargo build + 신규 unit 테스트 2개, `cargo test --test cum_return_parity_test` 5/5, `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-29 | - | success | 4A.1 follow-up — keyring crate platform feature 활성화. **증상**: 사용자가 키 입력 후 Save 시 "Saved access_key but read-back failed (got 0 bytes; err: None)" 에러. Save 자체는 성공한 듯 보였으나 직후 read에서 NoEntry. **원인**: `keyring = "3"` 만 추가하면 default가 **mock backend(no-op)** — 실제 OS credential store에 안 닿음. `set_password` 무시, `get_password` 항상 NoEntry 반환. **수정**: Cargo.toml에 platform별 features 분기 — `cfg(target_os="windows")` → `windows-native`, macOS → `apple-native`, Linux → `sync-secret-service` + `crypto-rust`. 직전 작업의 read-back 검증이 silent corruption을 첫 시도에서 잡아낸 케이스(평소 mock backend는 에러 없이 통과해버림). `cargo build --features tauri-app` 통과. |
| 2026-04-29 | - | success | Phase 4A.1 — Upbit API 키 OS 키체인 저장. **배경**: 마스터플랜 [plans/master/20260429_Real_Auto_Trading.md](plans/master/20260429_Real_Auto_Trading.md) 작성 후 첫 단계. 이전엔 `UPBIT_ACCESS_KEY`/`SECRET_KEY` 환경변수만 지원 → 사용자가 매 실행 시 env 설정 필요 + 평문 위험. **변경**: (1) Cargo.toml에 `keyring = "3"` 추가 — Windows Credential Manager / macOS Keychain / Linux Secret Service. (2) [commands/upbit_keys.rs](src-tauri/src/commands/upbit_keys.rs) 신규 — `save_upbit_keys` / `get_upbit_key_status` / `clear_upbit_keys` / `test_upbit_connection` 4개 Tauri 커맨드. `load_upbit_keys()` 헬퍼가 keyring → env var → none 순서로 해결. (3) UpbitClient 생성 경로(`commands/trading.rs`, `services/auto_trader.rs`, `services/live_scheduler.rs`, `commands/data.rs`, `server/routes.rs`) 5곳을 `upbit_keys::load_upbit_keys`/`upbit_client_or_err`로 일원화 — env-var fallback 유지(개발 호환). (4) 프런트 [lib/api.ts](src/lib/api.ts)에 `saveUpbitKeys`/`getUpbitKeyStatus`/`clearUpbitKeys`/`testUpbitConnection` + `UpbitKeyStatus` 타입. (5) [SettingsPage.tsx](src/pages/SettingsPage.tsx) "Upbit API Keys" 카드 — 상태 배지(Configured/Not configured + source), Save/Test/Clear 버튼, 결과 메시지, 키 값은 화면에 절대 노출 안 함. **검증**: `cargo build --features tauri-app` 통과, `cargo test --test cum_return_parity_test` 5/5, `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-28 | - | success | 세션 팔레트 의미 충돌 해소 — 캔들 상승봉(초록)/매도 수익(파랑)/하락봉·매도 손실(빨강)이 차지한 hue를 세션 식별 팔레트가 침범해 "어느 세션인지"와 "수익/손실"이 같은 색으로 겹쳤음. Tableau 10에서 충돌 5색(슬레이트블루/빨강/초록/청록/살구) 제거 + ColorBrewer Dark2의 안전 5색(진보라 #7570B3 / 마젠타 #E7298A / 진주황 #D95F02 / 골드 #A6761D / 진회색 #666666) 보충. 최종 10색: 주황/노랑/라일락/갈색/진보라/마젠타/진주황/골드/회색/진회색. 처음 5개는 채도 높은 hue로 동시 5~6 세션도 시각 구분 명확. [sessionPalette.ts](src/components/live/charts/sessionPalette.ts). 호출자(`colorFor`) 시그니처 동일 → SessionTable/CandleChart/SignalStripChart 모두 자동 적용. `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-28 | - | success | 캔들차트 sell 마커: 화살표를 봉 가까이로 — 직전 변경에선 이름 마커가 화살표를 갖고 있어 stack 후 "이름→화살표→수익률" 순으로 그려짐. 사용자 요구는 "이름→수익률→화살표(봉 가까이)". 원리: lightweight-charts는 (a) 마지막 push가 위로 쌓이고 (b) 한 마커는 텍스트 위 / shape 아래 순. 화살표가 가장 아래에 오려면 **먼저 push되는 마커가 arrow를 가져야** 함. **수정**: (1) 수익률 마커에 arrowDown 부여(먼저 push, 아래쪽), (2) 이름 마커는 shape='square' size=0으로 invisible(나중 push, 위쪽). 결과 stack 순서: 이름 → [invisible] → 수익률 → ↓ → 봉. [CandleChart.tsx](src/components/live/CandleChart.tsx). `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-28 | - | success | 캔들차트 sell 마커 stack 순서/모양 보정 — 직전 변경에서 stack 순서가 반대로 나옴(`+3.01%` 위, 이름 아래). 원인: lightweight-charts가 같은 time/position에서 **마지막 push된 마커를 위쪽으로** 쌓음. **수정**: push 순서 뒤집어 (1) 수익률(아래) → (2) 이름(위). 추가로 수익률 마커의 shape을 `'square'` size=`0`으로 변경해 화살표 중복 시각 노이즈 최소화 — 이름 마커의 화살표만 봉을 가리키고 수익률 마커는 텍스트 위주로 보이게. [CandleChart.tsx](src/components/live/CandleChart.tsx). `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-28 | - | success | 캔들차트 sell 마커: 두 줄 stack로 색 분리 — 1차에서 `\n` 단일 마커로 두 줄 처리했으나 lightweight-charts는 마커당 단일 color (텍스트/화살표 공유) 라 줄별 다른 색이 불가능. **수정**: sell + pnl_pct 있을 때 같은 time/position에 마커 두 개 push — (1) session 고유 색 + 라벨(`Short_156%`), (2) 수익=파랑(blue-500 #3b82f6) / 손실=빨강(rose-500 #f43f5e) + 수익률(`+1.93%`). 한국 주식 관행대로 수익 파랑. buy 마커는 기존 단일 마커 유지. [CandleChart.tsx:253-294](src/components/live/CandleChart.tsx#L253-L294). `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-28 | - | success | 캔들차트 sell 마커에 수익률 표시 + 색상 분기 — buy는 기존대로 session 고유 색 + 라벨, sell은 (1) pnl_pct 부호에 따라 화살표/텍스트 색을 emerald-500(+) / rose-500(−)로 변경, (2) 라벨 두번째 줄에 `+1.93%` / `−0.47%` 형태로 수익률 표시. [CandleChart.tsx:253-285](src/components/live/CandleChart.tsx#L253-L285)에서 marker 빌드 루프만 수정 — sell이고 pnl_pct가 있으면 color/text override, 그 외엔 기존 동작 유지. `pnl_pct`는 ratio(0.0193 = +1.93%)이므로 *100 곱해 표시. lightweight-charts marker text의 `\n` 줄바꿈으로 두 줄 라벨. `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-28 | - | success | SessionTable 컬럼 정렬 수정 — 헤더 "UnrealizedSignal" / 값 "-0.18%holding" 이 붙어 보이던 시각 충돌 해소. 원인: 인접 컬럼이 `text-right` (Unrealized) + `text-left` (Signal) 정렬 + 셀 간 padding 0 → 텍스트가 셀 경계에서 직접 만남. **수정**: [SessionTable.tsx:34](src/components/live/SessionTable.tsx#L34) `<table>` className에 Tailwind arbitrary variant `[&_th]:px-3 [&_td]:px-3` 한 줄 추가 — 모든 th/td 가로 padding 통일. 별도 셀 단위 수정 불필요. **DB 측 검증**: `live_sessions.live_return = 0.0` (세 세션), `real_started_at` 이후 sell trades = 0 — 백엔드는 정확. 사용자 화면의 큰 Equity 값은 직전 변경(deriveSessionPnl baseEquity 재정의)이 프런트에 미반영된 결과 → 앱 재시작/리로드 시 정상화. `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-28 | - | success | 라이브 화면 Equity 의미 통일 — "Start 후 라이브 자본"으로 변경. **문제**: 이전 구현은 Equity 컬럼이 백엔드 `current_equity`(since~now 통합)를 그대로 표시해 "Live=+0%인데 Equity=8,596,343 (+759%)" 같은 시간축 모순이 발생. baseline 시뮬 결과와 라이브 누적이 한 컬럼에 섞여 사용자가 "내가 실제로 가진 자본"을 못 읽음. **수정**: [liveTradingStore.ts:231](src/stores/liveTradingStore.ts#L231) `deriveSessionPnl`의 baseEquity를 `current_equity` → `initial_capital × (1 + live_return/100)` 로 변경. holding 중에는 그 baseEquity에 `tick/buy_price` 비율을 곱해 미실현 마크-투-마켓. 결과적으로 Equity = "Start 시점 자본 × Live 누적 비율 × 미실현 비율" → 모든 컬럼이 Live 시간축으로 통일. 백엔드 `current_equity`는 진단/equity-curve 용도로 그대로 보존(읽기만 안 함). **검증**: `npx tsc -b` exit 0, `npm run vite:build` 통과. |
| 2026-04-28 | - | success | 라이브: Baseline (preset) vs Live (Start 후) 분리 측정/표시. **배경**: 라이브 화면이 since~now 통합 누적만 보여줘서 백테스트 결과와 라이브 발생분이 한 값에 섞여 있었음 (+316% 같은 큰 수치 = 사실상 백테스트 결과). 사용자 요구: "프리셋의 default 정보 + Start 후 수익률을 비교 가능하게". **변경**: (1) **마이그레이션 009** — `presets.baseline_return/baseline_trades`(저장 시점의 백테스트 메트릭, 정적), `live_sessions.live_return`(real_started_at 이후 trades 누적 %, 동적), 기존 세션 `real_started_at IS NULL` backfill. (2) **백엔드** — `SavePresetArgs`/`insert_preset`에 baseline_* 인자 추가. `insert_session`이 `real_started_at = now()` 자동 set (start_ts는 preset.since_ts 그대로). [session_engine.rs](src-tauri/src/services/session_engine.rs)가 매 사이클마다 `result.trades`에서 `t.sell_timestamp > real_started_at` 필터링해 `live_factor = ∏(1 + pnl − 2f)` → `live_return` 별도 저장. `update_session_cycle` 시그니처에 `live_return` 추가. (3) **프런트** — `Preset.baseline_*`/`LiveSession.live_return` 타입 동기화. `SimulationPage.handleSavePreset`이 `result.total_return/total_trades`를 함께 전송. `NewSessionDialog`가 선택된 preset의 백테스트 기간/수익률/거래수를 미리보기. `SessionTable`에 **Backtest** + **Live** 두 컬럼 분리(기존 Total P/L 대체). preset에 메트릭이 없으면 "—" 표시. **검증**: `cargo build --features tauri-app` 통과, `cargo test --test cum_return_parity_test` 5/5 통과, `npm run vite:build` 통과 (TS 0 errors). |
| 2026-04-28 | - | success | 라이브 차트 멀티 세션 표시 토글 추가 — 기존엔 캔들 마커는 모든 세션 동시 표시 / 신호 스트립은 단일 세션만으로 UI 일관성 깨짐. (1) `liveTradingStore`에 `hiddenSessionIds: number[]` + `toggleSessionVisibility` / `setAllSessionsVisible` 액션 추가 (음의 집합 패턴 — 빈 배열 = 전부 표시, 새 세션 자동 visible). (2) `CandleChart`에 `hiddenSessionIds` prop 추가, 마커 루프에서 `Set.has`로 skip. 색 팔레트는 전체 sessions로 계산해 토글 시 색 안정. (3) `LiveTradingPage`에 칩 토글바(색 사각형 + 라벨, opacity로 hidden 표시, All/None 버튼). (4) Signal session `<select>` 옵션을 visible 세션으로 제한, 현재 선택이 hidden되면 첫 visible로 자동 폴백. (5) `SessionTable`에 "Show" 체크박스 컬럼 추가 — 칩과 동일한 store 액션 공유. `vite:build` 통과 |
| 2026-04-16 | - | success | CLAUDE.md 생성 — 코드베이스 분석 후 빌드 명령, 아키텍처, 주요 패턴 정리 |
| 2026-04-16 | - | success | CLAUDE.md 개선 — Bitcoin 프로젝트 CLAUDE.md 참고하여 한글화, 전략 테이블, 실수 방지 체크리스트, 필수 준수 사항 추가 |
| 2026-04-16 | - | success | 레거시(C# WinForms) vs 신규(Tauri+React+Rust) 완성도 비교 평가 — 종합 85%, 자동매매 루프·ML 모듈이 핵심 갭 |
| 2026-04-16 | - | success | 자동매매 루프 + 데이터 자동 업데이트 구현 — auto_trader.rs 신규, 전략 signal 수정(V0-V5), 프론트엔드 UI 업데이트, 42개 테스트 추가 (총 209개 통과) |
| 2026-04-20 | - | success | Market Data 3-종 픽스 — (1) BTC/hour 부분 데이터 자동 백필(`sync_market`이 oldest 비교해 backward fetch), (2) 시작 시 백그라운드 업데이터(`services/market_updater.rs`, 60초 주기), (3) 차트 x축 timeframe별 라벨 포맷(hour: `MM-DD HH:00`, day/week: `YYYY-MM-DD`) |
| 2026-04-20 | - | success | 차트 가상화 + Live Price — `get_candles`에 `limit` 추가(SQL: DESC LIMIT N → ASC, 기본 500), DataLoadPage에 100/500/1000/5000 셀렉터. `getCurrentPrice` 2초 폴링 → `series.update()`로 마지막 봉 close/high/low 갱신. 백그라운드 업데이트 시 스크롤 점프 방지(첫 로드만 fitContent) |
| 2026-04-20 | - | success | Live 갱신 + 봉 데이터 정확도 픽스 — (1) `get_current_price`가 인증키 없이도 동작하도록 `create_public_client()` 분리 (이전엔 `UPBIT_ACCESS_KEY` 미설정 시 폴링 silent fail), (2) `INSERT OR IGNORE` → UPSERT(`ON CONFLICT DO UPDATE`)로 변경 — 진행 중·방금 닫힌 봉의 high/low/close가 매 fetch마다 갱신됨. (3) DataLoadPage에 30초 주기 `refreshCandles` — UPSERT 보정 결과가 차트에 자동 반영 |
| 2026-04-20 | - | success | 시뮬레이션 파라미터 UI 노출 — 레거시 C# `TradingParametersControl` 대응. 근본 원인: `list_strategies`가 `parameter_ranges()` 결과를 프론트에 실어 나르지 않아 UI가 스키마를 몰랐음. 수정: `StrategyInfo`에 `ranges`+`defaults` 필드 추가(커맨드 + Axum 라우트 양쪽), `Registry::list()`가 `(key, name, ranges)` 반환, `simulationStore`에 `params`/`setParam`/`resetParams` 추가, `SimulationPage`가 이름 prefix(`v{N}_*`, `buy_*`, `sell_*`) 기반 섹션 자동 분류 후 `min/max/step` Input 렌더. 테스트 3개 튜플 해체 패턴 업데이트(전체 61 lib + 47 integration 통과) |
| 2026-04-20 | - | success | 시뮬레이션 default 값 + 결과 상세화 — (1) `TradingParameters::default()` V3 보간식 33개 필드를 레거시 `TradingConfig_V3_RegimeAdaptive_20260401_140149.json` 값으로 채움(기존 전부 0.0 → V3 시뮬이 거래 0건이던 문제 해결). (2) SimulationPage MetricCard 5→10개로 확장: Market Return, Profit Factor, Sortino Ratio, Annual Return, Max Consec. Losses 추가(기존 SimulationResult 필드 활용, 새 API 호출 없음). 테스트 113개 전부 통과 |
| 2026-04-20 | - | success | V0 default 엔진 의미 불일치 수정 — V0 시뮬이 Market Return 1204% 상황에서도 Trades=0 나오던 근본 원인. 엔진 [core/engine.rs:55-65](src-tauri/src/core/engine.rs#L55-L65)는 `price_change <= -urgent_buy_price_drop_ratio`로 percent threshold 해석(`ParameterRange` min=0.001 max=0.1). 그런데 `Default` 구현이 레거시 C# multiplier 값 `1.0`을 그대로 옮겨놔서 "100% 하락 시 매수"라는 불가능 조건 → 6년치 캔들에서도 트리거 0회. 수정: `urgent_buy_price_drop_ratio` 1.0→0.02, `buy_ready_price_drop_ratio` 1.0→0.01, `urgent_sell_profit_ratio` 1.0→0.03, `sell_ready_price_rise_ratio` 1.0→0.01, `fee_rate` 0.0→0.0005 (Upbit 기본 수수료). 테스트 116개 통과 |
| 2026-04-20 | - | success | V0/V1/V3 default volume threshold 현실화 + 회귀 테스트 추가 — 실 SQLite DB 쿼리 결과 BTC/hour volume avg=221 BTC, max=8224 BTC. 그런데 `urgent_buy_volume_threshold=30000`, `buy_ready=9000` (BTC 단위) → 역사적으로 한 번도 초과 불가 → Trades=0. 원인: 레거시 C# 값을 단위 검증 없이 그대로 이식. 수정: (1) V0 default volume threshold 30000→1000, 9000→200, decay_ratio 0.02→0.3. (2) V0/V1 `parameter_ranges()` min/max 실 분포 기준 재설정. (3) V3 default 전면 재계산 — 레거시 multiplier(1.045, 5000 BTC)를 Rust percent/BTC 의미로 매핑(0.005~0.02 percent, 300~1500 BTC). (4) 신규 `tests/real_data_test.rs` — LOCALAPPDATA DB 발견 시 V0/V1/V3 default로 실 BTC/hour 돌려 trades>0 회귀 검증. 전체 211개 테스트 통과 |
| 2026-04-20 | - | success | 시뮬레이션 페이지 3개 기능 추가 — (1) Market(BTC/ETH) + Timeframe(hour/day/week) + Since/Until 날짜 picker를 시뮬레이션 페이지 자체 컨트롤로 분리 (`marketDataStore` 의존 제거). (2) 백엔드 `load_candles_range` 신규 — SQL WHERE에 `timestamp >= since AND <= until` 바인딩. `run_simulation` Tauri 커맨드 + `/api/simulation/run` Axum 라우트 양쪽에 `since/until: Option<String>` 전파. (3) **상태 전이 시그널 로그 복원** — 레거시 `NetTradingEngine.DetermineSignalType` 포팅. 신규 `SignalEvent { index, timestamp, signal_type, price, position }`, `SimulationResult.signal_log` 필드. 상태 변경 시에만 push (6종 분류: ready/buy ready/buy/hold/sell ready/sell). V0~V5 전체 엔진에 적용(V0/V1 자동, V3/V5는 buy_sign 추적, V2/V4는 position 전이만). UI: Trade History에 `Buy Signal` 컬럼 분리 추가, 신규 "Signal Timeline" 카드(배지 색상 구분 + sticky 헤더 + 400px 스크롤). 전체 테스트 211개 통과 |
| 2026-04-21 | - | **success** | **V3 legacy 909.29% 완전 재현 (bit-exact parity) 달성** — legacy `RegimeAdaptiveStrategy.cs`에 임시 trace writer(env-var 게이트) 주입 후 xUnit dotnet test로 10,878-bar CSV dump 생성. Rust V3도 동일 trace를 dump해 bar-level diff 수행. 2개 근본 원인 발견: (1) **RSI warmup scope** — legacy는 `CalculateAll(window)`로 window 내부에서 indicator 시작(첫 15 bar RSI=0→50 fallback), Rust는 full history로 이미 성숙. test에서 window 기준 재계산으로 해결. (2) **PsyDay 소스** — legacy `MarketDataConverter`가 `merged_data_hour.csv`의 `ETH_day_new_psy` column을 **그대로** 사용. 이 column은 실제 day-scale PSY와 fallback hour-PSY가 혼재 (일부 날짜만 fallback 적용됨). Rust는 CSV에서 직접 column 파싱해 date별 last non-zero 값을 map으로 주입. **결과**: trace diff rows = **0 (10,878 bar 완전 일치)**, trades=**59** (legacy 59 ✓), total_return=**909.29%** (legacy 909.29% ✓), max_drawdown=**10.67%** (legacy 10.67% ✓). 작업 후 legacy source 원복 + diagnostic test `#[ignore]`. 전체 228개 테스트 통과 |
| 2026-04-21 | - | partial | V3 남은 14% 격차(589→909) 상세 추적 — 8 legacy-only + 11 rust-only 각 bar 분석. (1) **Data 일치 확인**: 5개 miss bar 모두 OHLCV 완벽 일치 (Rust vol=834.54, legacy vol=834.54 등). CSV 주입된 DB와 legacy 원본 동일. (2) **Volume 조건 miss**: 예 2025-12-06 05:00 vol=835 < dyn_buy_vol=5105 → Rust 진입 불가. Legacy는 동일 bar에서 진입. RSI/PSY 공식 동일하지만 **phase-shift**로 다른 cycle 생성. (3) **근본 패턴 — cycle drift**: Rust가 12-05 03:00에 **extra 매수** (legacy에 없음) → 12-06 05:00 매도. 같은 12-06 05:00에 Legacy는 매수 (Rust는 직전 매도로 idle 아님). 한 cycle 어긋남 → 이후 state machine이 서로 다른 경로. 이는 엔진 버그가 아닌 confirm 조건의 미세한 threshold 계산 차이 누적 (IEEE 754 double 연산 순서/누적 오차 또는 indicator 계산 중간값의 미세 불일치). 단일 fix 지점 없음. 현재 **matched 51/59 (86%), exit-shift 0h** 는 엔진 parity가 구조적으로 완벽함을 증명. 나머지 14%는 legacy C# 엔진과 bit-exact 재현하려면 legacy binary 실행으로 bar-level trace 대조 필요 |
| 2026-04-21 | - | success | Legacy CSV 주입 + PsyDay 의미 수정으로 589% 달성 — 사용자 제공 `D:\SW\Bitcoin\merged_data_hour.csv` (48,652 bars) 와 `merged_data_day.csv` (2,147 bars) 를 SQLite `market_data`에 주입 (KST→UTC 변환). Legacy 코드 정밀 재분석 결과 `MarketDataConverter.cs:54-69`에서 **`MarketData.PsyDay = dayPsyByDate[previousDate]`** 발견 — 우리 Rust가 쓰던 hour-scale PSY가 아니라 **"전일의 마지막 non-zero hour-PSY"** (day CSV column 0이면 fallback 경로). `calculate_all_with_day_psy` 추가, hour PSY 기반으로 KST-date별 마지막 값 map → `psy_day[i] = map[kst_date(i) - 1일]` 매핑. **결과 급등**: matched 42→**51** (86%), legacy-only 17→**8**, rust-only 9→**11**, exit-shift 0h 유지. 수익 278% → **589.86%** (legacy 909% 대비 65%). 남은 8 entry miss는 Q4 2025~Q1 2026의 +12%·+14% high-profit trades로 복리 차이가 주요 격차 원인. 전체 227개 테스트 통과 |
| 2026-04-24 | - | success | Trade History open position 행 추가 — 사용자가 Cum % vs Total Return 불일치 재차 지적. 신규 `tests/cum_return_parity_test.rs` (5 tests, 순수 수학 diagnostic) 작성. 결과: (1) 닫힌 trade만 있으면 exact formula (c) `(1-f)·(c_{m-1}/c_k)·(c_m/c_{m-1}-f)`가 엔진과 1e-12 수준 일치, (2) formula (b) 드리프트는 거래당 bps 수준, (3) **"open position at end" 하나만으로 14.58% vs 0.88% 격차** — 엔진 [regime_adaptive.rs:280](src-tauri/src/strategies/regime_adaptive.rs#L280)는 sell 체결 시에만 TradeRecord push하지만 bar_return은 매 봉 누적돼서 미청산 포지션의 unrealized가 Total Return에만 반영됨. **수정**: [SimulationPage.tsx](src/pages/SimulationPage.tsx) 프론트엔드만 변경 — `result.last_position === 1`이면 `openLeg` useMemo에서 `engineFactor / closedFactor - 1`로 unrealized 역산, Trade History 말미에 "open" 뱃지 달린 synthetic row 추가. 마지막 Cum % = `result.total_return` 정확히 일치. 엔진 0 변경(`last_position`, `last_buy_price` 기존 필드 재활용). `vite:build` + cum_return_parity 5 tests 통과 |
| 2026-04-24 | - | success | Trade History Cum % 수수료 반영 — 사용자 지적: Total Return(bar-based, 수수료 포함)과 Cum %(trade-based gross)가 달라 실제 수익률 혼란. 엔진이 진입/청산봉에서 각각 `fee_rate` 차감하는 모델과 맞추기 위해 `cumReturns` useMemo에 `feeDrag = 2 × fee_rate` 적용: `acc *= (1 + pnl_pct - feeDrag)`. Fee rate는 전략별 키(`params.v31_fee_rate ?? params.v3_fee_rate ?? 0`) fallback. 결과: Trade History 마지막 행 Cum %가 Total Return 메트릭과 1~2bps 수준으로 일치 (bar-by-bar vs trade-by-trade 복리 순서의 고차 오차만 남음). V3 legacy parity 유지(엔진 pnl_pct는 fee 미반영 gross 그대로). `vite:build` 성공 |
| 2026-04-24 | - | success | Trade History에 누적 수익률(Cum %) 컬럼 추가 — 0번 거래 시작 equity=1.0 기준 복리 누적. [SimulationPage.tsx](src/pages/SimulationPage.tsx) `useMemo`로 `cumReturns: number[]` 계산 (`acc *= 1 + t.pnl_pct; return acc - 1;`), 이는 [PerformanceChart.tsx:10-15](src/components/charts/PerformanceChart.tsx#L10-L15)의 equity curve 공식과 동일. PnL % 바로 뒤에 "Cum %" 컬럼 삽입, 양/음에 따라 emerald/rose 텍스트 색(PnL 셀처럼 배지는 아니라 구분). 테이블 컬럼 9 → 10개. `vite:build` 성공 |
| 2026-04-24 | - | success | Simulation 페이지 sticky 바 padding peek-through 수정 — [App.tsx:234](src/App.tsx#L234)의 `<main className="p-6">` 때문에 scrollport(=content-box)가 border-box보다 24px 아래에서 시작. `position:sticky; top:0`은 scrollport 기준이라 스크롤 시 상단 24px padding 영역에 콘텐츠가 bleed. **해결**: sticky 바 클래스 `top-0` → `-top-6` (= `top:-24px`). CSS spec상 negative sticky top 허용. 스크롤 시 바의 border-box top이 scrollport y=-24 (= main border-box y=0)에 고정돼 padding 갭을 덮음. `-mt-6 pt-6` 조합은 scroll=0 상태에서 이미 같은 위치였으므로 시각적 변화는 스크롤 상태에서만 발생. Params sticky 위치도 동기화(`lg:top-[150px]` → `lg:top-[126px]`) — top 바가 24px 위로 이동한 만큼 Params도 당겨 간격 유지. `vite:build` 성공 |
| 2026-04-23 | - | success | Simulation 페이지 공유 collapse 토글 — Params 개별 chevron 제거, 2열 grid **밖** 상단에 공유 토글 버튼 추가. 상태 `paramsOpen`→`simulationOpen`으로 rename, `{simulationOpen && (<div className="grid...">)}`로 Params + Metrics + Equity 3영역을 함께 접고 펼침. Params CardHeader는 chevron·cursor-pointer·onClick 제거(제목 + Reset 버튼만). 공유 토글은 `(currentStrategy \|\| result) && ...` 조건으로 초기 빈 상태에선 안 보임. 버튼에 `select-none` + hover color 전이 적용. Trade History·Signal Timeline은 기존대로 각자 자체 collapse 유지(풀폭 대용량 영역). sticky 동작·2열 레이아웃·차트 높이 모두 그대로 보존. `vite:build` 성공 |
| 2026-04-23 | - | success | Equity Curve 차트 높이 viewport 반응형 — [PerformanceChart.tsx:70](src/components/charts/PerformanceChart.tsx#L70)의 `height: 300` 하드코딩을 `height: "100%", width: "100%", minHeight: 280`으로 변경(`echarts-for-react`가 부모 높이에 fill되도록). [SimulationPage.tsx](src/pages/SimulationPage.tsx) Equity CardContent에 `h-[400px] lg:h-[calc(100vh-540px)] lg:min-h-[360px]` — desktop(lg+)에서는 sticky 헤더(150) + metric 2행(280) + 간격/헤더(110) 제외한 나머지 viewport 공간을 차트가 채움. 좁은 창에서는 min-h 360px guard. `vite:build` 성공 |
| 2026-04-23 | - | success | Simulation 페이지 2열 레이아웃 (A안: 비대칭 Split) — 루트에 `grid lg:grid-cols-[minmax(340px,38%)_1fr] items-start` 도입. **좌측** Parameters 카드를 래퍼 div로 감싸 `lg:sticky lg:top-[150px]`, CardContent에 `lg:max-h-[calc(100vh-220px)] lg:overflow-y-auto` — sticky 헤더 아래 고정되며 파라미터가 많으면 카드 내부에서만 스크롤. 파라미터 내부 grid는 3열→2열(`grid-cols-1 md:grid-cols-2`)로 좁은 칼럼에 맞춤. **우측** 결과 영역을 `{result && (...)}` 2개로 분리: (1) 메트릭 카드 10개 + Equity Curve는 우측 칼럼 내부(`space-y-6 min-w-0`), 메트릭 grid는 `grid-cols-2 md:grid-cols-3 xl:grid-cols-5`로 좁은 폭 대응. (2) Trade History(9컬럼) + Signal Timeline은 2열 grid **밖** 풀폭으로 유지 — 와이드 테이블이 찌그러지지 않도록. 우측 셀에 `min-w-0` 삽입(grid 자식 `min-width:auto` 기본값이 테이블로 인해 트랙 부풀림 방지). `vite:build` 성공(2366 modules, 1.02s) |
| 2026-04-23 | - | success | V5 Enhanced Adaptive 전략 추가 (레거시 C# 충실 재포팅) — 기존 git history의 Rust V5 구현은 C# V5와 로직이 완전히 달라(PSY-hour 단일+ATR trailing vs 듀얼 PSY 확정) 폐기하고, `regime_adaptive.rs`를 템플릿으로 V3+듀얼 PSY 방식으로 신규 작성. **Diff는 단 2곳**: (1) 매수 확정 `psy_day < buy_psy` → `psy_hour < buy_psy_hour && psy_day < buy_psy_day`, (2) 매도 확정 `vol < set_volume*sell_decay` → 같은 조건 + `psy_hour > sell_psy_hour && psy_day > sell_psy_day`. 나머지(urgent buy, fixed SL, max hold, volume cutoff, bar-by-bar returns)는 V3와 완전 동일. `TradingParameters`에 V5 전용 12개 PSY 필드 + serde default 추가(V3→V5 config 호환). 레거시 C# `NetTradingEngine.cs:254-279` 기본값 그대로(`V5BuyPsyHourLo=0.05, Hi=-0.15` 등) 이식. Strategy trait 구현 + `StrategyRegistry`에 "V5" 등록. **회귀 테스트 6개 신규**: (1) Registry 등록, (2) parameter_ranges가 V5 PSY 12개 포함 & v3_buy_psy_* 미포함, (3) 느슨한 PSY로 V5 신호 ≥ V3, (4) 엄격한 PSY로 decay-path 매수 전면 차단, (5) **듀얼 AND 조건 검증** (hour만 통과해도 차단), (6) V5가 V3 침해하지 않음. **전체 테스트 통과**(V3 legacy 회귀 테스트 포함 0 regression). 프론트 `vite:build` 성공 — `list_strategies`가 자동으로 V5를 UI에 노출 |
| 2026-04-21 | - | success | V3 engine 전면 재작성 (legacy 1:1 포팅) — 3가지 중대 누락 발견·수정: (1) `buyPrice = current.Close` — 매수가에 fee 추가 없음 (Rust는 `close + close*fee` 추가했었음). (2) `sellPrice = current.Close` — 매도가에도 fee 추가 없음. (3) 수익률 **bar-by-bar shift 패턴**: `barReturn = previousPosition × dailyReturn`, entry/exit bar에서 `-= fee_rate`, `totalReturn = prod(1 + barReturn) - 1` (V0/V1의 trade-based와 완전 다름). 또한 `safe_rsi(rsi>0 ? rsi : 50)` warmup, entry_rsi default 50 (legacy), 테스트 window를 legacy JSON의 StartDate/EndDate(2025-01-01T18:00 ~ 2026-03-31T17:00 KST)로 정확 매칭. **결과: Rust trade #1-#9이 legacy와 price까지 완벽 일치 (5,018,000 / 4,733,000 / 4,060,000 등). matched=38(29→38), exit-shift=0h(29h→0h perfect), 수익률 57% → 212.51%**. 남은 격차(legacy 909% vs Rust 212%)는 21개 entry miss — 조사 결과 RSI 값 1-2 units 차이로 `buy_ready_vol` interpolation이 knife-edge에서 갈림(예: vol=8596, RSI=62→진입, RSI=63→miss). Upbit API data snapshot drift 또는 indicator seed 기간 차이로 추정 — 근본 해결하려면 legacy 저자 CSV 직접 주입 필요. 225개 테스트 전부 통과 |
| 2026-04-21 | - | success | V3 PSY 공식을 legacy weighted NewPsy로 정확 포팅 — legacy C# `DataUpdateManager.cs:294-318`의 PSY 계산이 단순 up/down count가 아니라 **price-move magnitude 가중치**: `(upD × upC/t - downD × downC/t) / period`. period=10, hourly에서 `ETH_Day_NewPsy ← ETH_Hour_NewPsy` fallback. Rust `calc_psy`를 verbatim 포팅, `psy_day = psy_hour.clone()`으로 fallback도 반영. `tests/v3_legacy_config_test.rs`에 trade-level diff 테스트 추가 — legacy_V3.txt를 파싱해 bar-by-bar 매칭. 결과: 59 vs 58 trades, **matched=29 (from 25)**, legacy-only=30, rust-only=29, avg exit-shift +29h. 완전 parity는 여전히 미달 (수익 48→57%, legacy 909% 대비). 남은 격차는 매도 경로 순서/타이밍 + 일부 entry miss. indicators_test, scenario_tests PSY assertion 업데이트. 전체 225개 테스트 통과 |
| 2026-04-21 | - | success | V3 legacy parity 2중 수정: 타임존 + PSY 스케일 — 사용자 제공 `legacy_V3.txt` 로그 (KST 타임스탬프, 2025-01-01~2026-04-01) 기준 bar-level 비교. 2개 근본 원인 발견·수정: (1) **Timezone mismatch** — legacy 로그가 KST, Rust DB는 UTC. window bound를 `2024-12-31T15:00~2026-03-31T15:00 UTC` (= KST 2025-01-01~2026-04-01)로 정렬. Rust trade #1 = legacy trade #4 (2025-02-03 10:00 KST). (2) **PSY 스케일 mismatch** — legacy는 `(up-down)/period` 로 `[-1, +1]` 범위 (threshold 0.14/-0.24 의미 있음), Rust는 `up/period × 100` 으로 `[0, 100]`이라 `psy < 0.14` 가 거의 항상 false → decay_buy confirm 대부분 차단. `calc_psy`를 legacy 공식으로 재작성: `out[i] = (up - down) / period`. 결과: **Rust 9 trades → 56 trades로 급증**, trade #1-#5가 legacy와 timestamp/price 거의 일치 (예: #1 buy 2025-01-08 19:00 @ 5,020,509 vs legacy 5,018,000). 총 수익 23% → 48.59% (legacy 909% 대비 여전히 격차는 optimizer in-sample + 매도 경로 세부 잔차). indicators_test/scenario_tests PSY 기대값을 [-1,+1]로 업데이트. 전체 224개 테스트 통과 |
| 2026-04-21 | - | partial | V3 909% 수익 차이 추적 — 사용자 지적 "legacy가 900%+" 의 출처를 JSON 파일에서 발견: `TradingConfig_V3_RegimeAdaptive_20260401_140149.json:1083` 에 `TotalReturn: 909.29%, WinRate: 81.36%, MaxDD: 10.67%, Trades: 59` 기록됨 — **legacy NSGA-II optimizer가 저자의 당시 ETH 데이터에서 탐색한 in-sample 결과**. 우리 Rust 결과: 65 trades, 140%, win 54%, MaxDD 55%. 격차 원인 2가지 발견/수정: (1) legacy `goto CalculateReturns` 로직 — urgent_sell trigger bar에서 매도 skip하고 다음 bar confirm, buy/sell ready bar에서도 동일 iteration confirm 체크 skip. Rust 포팅이 이를 간과해 early-close 문제 발생. `regime_adaptive.rs`에 `continue` 분기로 정확히 포팅. (2) 남은 격차(140% vs 909%)는 **입력 데이터 + indicator 계산 미세 차이**로 추정 — legacy 저자의 DB snapshot과 우리 Upbit API DB가 candle/indicator 세부에서 미묘히 다름. 구조적 parity는 달성했으나 in-sample 수치 재현은 동일 fixture 필요. 전체 223개 테스트 통과 |
| 2026-04-21 | - | success | V3 legacy parity 재검증 (ETH 기반으로 수정) — 레거시 C# 프로젝트가 ETH 전용임을 반영. `v3_legacy_config_test`를 ETH/hour 데이터로 로드하도록 수정 + BTC/hour 미스매치도 대조 테스트로 추가(`v3_legacy_config_on_btc_shows_mismatch`). `real_data_test`에 `v3_default_produces_trades_on_real_eth_hour` 추가. **ETH/hour 결과: 55,221 봉에서 65 trades, 수익률 +152.82%, win rate 53.85%, profit factor 1.574, Calmar 2.75** — legacy JSON default가 ETH 분포(avg 2821, max 163686)와 완벽히 fit. **BTC/hour 결과: 0 trades** — ETH-tuned 값이 BTC에 적합 안 함을 확인. `default_for_market`을 전략별 base market 반영하도록 재작성: V0/V1/V5 = BTC base, V3 = ETH base, `default_for_market("ETH")`는 V0/V1/V5 volume만 ×13, `default_for_market("BTC")`는 V3 volume만 ÷13. 전체 222개 테스트 통과 |
| 2026-04-21 | - | success | V3 engine을 legacy multiplier 의미로 재작성 (C-1) — `regime_adaptive.rs` 완전 리팩토링: (1) `prev × price_drop_ratio > curr` (multiplier) 매수 조건, (2) urgent_buy 분기 신규 추가 (`V3UrgentBuyVolume` 필드 3개 추가), (3) legacy counter-reset 패턴 (buyPo 리셋이 buy-ready 볼륨 서지 기준), (4) 매도 측 `curr < buy × stop_loss_ratio` cutoff + `curr > buy × profit_ratio` urgent_sell (multiplier), (5) fixed_sl만 percent (legacy 동일), (6) hardcoded constants 포팅: sell_wait_max=168, cutoff vol mul=1.0, urgent confirm vol=1× sell-ready, (7) sell params가 `entry_rsi` 기준 frozen interpolation (legacy 동일). Parameter ranges도 multiplier 단위로 재정의 (ratio 0.8~1.1 등). 합성 데이터 scenario test에서 41 trades 생성 확인. 실 BTC/hour 데이터에서는 legacy JSON default 자체가 현 분포와 mismatch (volume 5000~17500 > max 8224; PSY threshold ~0 vs PsyDay range 0~100)라 trade 거의 없음 — 이는 engine이 아닌 **파라미터 자체의 문제**. `v3_legacy_config_test`이 feasibility 진단 출력. 종속 테스트(scenario_tests V3, real_data_test V3, indicators_test) 업데이트. 전체 220개 통과 |
| 2026-04-21 | - | success | V3 default를 legacy JSON 값과 동일하게 교체 + 상세 비교 — `TradingParameters::default` V3 섹션을 `TradingConfig_V3_RegimeAdaptive_20260401_140149.json`의 값 그대로 (volume 5000/17500, price_drop 1.045/1.025, stop_loss 0.85/0.82, profit 1.145/1.09 등) 설정. 신규 `tests/v3_legacy_config_test.rs`가 실 BTC/hour DB 55,220 bar에서 두 가지 모드를 나란히 실행: (A) literal legacy JSON 값 → Rust V3 engine, (B) 의미 변환된 값(`1-ratio`/`ratio-1`) → Rust V3 engine. 결과: **둘 다 trades=0** — (A)는 price_drop 1.045(>100% 하락 요구)가 근본 차단, (B)는 volume 임계값 5000~17500 BTC가 BTC/hour max(8224 BTC)를 초과해 entry 불가. 근본 원인 2가지: ① Rust V3 engine이 price_drop/stop_loss/profit을 percent로 해석 vs legacy는 multiplier. ② Rust V3 engine이 volume을 raw value로 체크 vs legacy는 rolling mean 기반(`V3VolumeLookback=35` 힌트). 완전 parity는 engine realignment 필요 — V3 엔진을 legacy 의미에 맞추거나 legacy V3 포팅. real_data_test와 indicators_test 종속 assertion 업데이트. 전체 220개 테스트 통과 |
| 2026-04-20 | - | success | V0 엔진 완전 legacy parity — 3가지 레거시 메커니즘을 Rust V0에 포팅: (1) `buy_immediate_volume_threshold` / `sell_immediate_volume_threshold` 필드 + counter-reset 로직 (대기 중 fresh volume surge가 wait 카운터 리셋), (2) `buy_confirm_psy_threshold` 필터 (default 1.0으로 off, 사용자가 낮추면 활성화), (3) `urgent_buy`/`urgent_sell`을 state machine 밖으로 빼 **unconditional**로 동작 — `buy_sign=1` 대기 중에도 급락+고볼륨이면 즉시 매수, `sell_sign=1` 대기 중에도 급등+고볼륨이면 즉시 매도 (레거시 C# 비대칭 설계 그대로). `legacy_parity_test.rs`의 `volatile_spikes` 시나리오를 결정적 3-cycle pattern으로 재설계. `TradingParameters::default`/`default_for_market` + optimizer `get_parameter`/`set_parameter` + V0/V1 `parameter_ranges()` + `indicators_test` 업데이트. 테스트 218개 전부 통과 (lib 61, scenario 50, integration 42, indicators 27, auto_trading 9, phase4 7, legacy_parity 7, strategy/engine/optimizer/real_data 기타) |
| 2026-04-20 | - | success | Legacy parity 테스트 추가 — 신규 `tests/legacy_parity_test.rs` (630줄). 레거시 `NetTradingEngine.RunSimulation`(V0) 전체를 Rust로 pure-function 포팅(multiplier 의미·PSY 확정·모든 risk gate·BuyImmediateVolume 카운터 리셋 포함). 파라미터 변환 헬퍼 `legacy_to_rust`가 `prev × ratio > curr` → `price_change ≤ -(1-ratio)` 매핑. 10개 결정론적 시나리오(uptrend/downtrend/sideways/V-shape/volatile spikes/low volume/bull-with-drawdown/crash-flat/gradual-climb/alternating pumps). 7개 테스트: V0 trade 수 parity, V0 수익 부호·크기 parity, V0 risk-gate 활동 parity(trade 수 차이는 구조적 차이—`BuyImmediateVolume` 부재—로 명시 허용), V0~V5(V4 제외) smoke/결정론/signal_log invariant/fee_rate monotonic. 전체 218개 테스트 통과 |
| 2026-04-20 | - | success | Since/Until ↔ Data 라벨 동기화 — `hydrateForMarket`이 `dataRange` 수신 후 `since/until`을 `min/max_timestamp.slice(0,10)`(yyyy-MM-dd)로 자동 채움. Market/Timeframe 변경 시마다 재설정되어 항상 data range와 입력 필드가 일치. Data 라벨도 `since || min_ts → until || max_ts` 로 표시해 사용자 수정 시 즉시 반영 |
| 2026-04-20 | - | success | WebView2 date picker 팝업 안 뜨는 이슈 수정 — `<input type="date">`가 "년-월-일" placeholder만 보이고 클릭해도 달력 UI가 열리지 않던 문제. WebView2는 input은 렌더하지만 클릭 시 native picker를 자동으로 띄우지 않음. [components/ui/Input.tsx](src/components/ui/Input.tsx)의 공통 `Input` 컴포넌트에 `type ∈ {date, datetime-local, time, month, week, color}` 감지 후 onClick에서 `HTMLInputElement.showPicker()` 명시 호출 추가. 모든 페이지의 해당 타입 input이 일괄 혜택 |
| 2026-04-22 | - | success | 숫자 입력 필드 "0 삭제 불가 + 020" 버그 수정 — 근본 원인: `<Input type="number" value={numberState} onChange={(e)=>set(Number(e.target.value))}>` 패턴에서 `Number("") === 0`이라 빈 문자열과 0이 합쳐져 backspace로 "0"을 지워도 state가 그대로 0 → React가 value={0}으로 재렌더 → DOM이 "0"으로 복원. 또한 "0" 앞에 숫자를 입력하면 브라우저 DOM value가 "020"으로 유지되어 React value prop과 불일치. 해결: 신규 공용 [components/ui/NumberInput.tsx](src/components/ui/NumberInput.tsx) — 내부 string buffer로 입력 텍스트 유지, 부모에게는 `onValueChange(n: number)`로 숫자만 전달. `useState(lastSyncedValue)` + render-time conditional setState 패턴(React 19 권장)으로 외부 reset 동기화. `isIntermediate("-", ".", "-.")` 가드로 음수/소수 입력 중간 상태 허용. blur 시 빈/NaN → `min ?? 0`으로 clamp. 마이그레이션: `OptimizationPage.tsx` 8개 input, `SimulationPage.tsx` 전략 파라미터 input — 기존 `Number(e.target.value)` 패턴 9곳 제거. `ManualOrderDialog`(string state)와 `DataLoadPage`(select)는 영향 없어 제외. 빌드·타입체크·lint 통과 |
| 2026-04-20 | - | success | 시뮬레이션 4개 개선 — (1) **Market별 default** 분리: `TradingParameters::default_for_market(market)` 신규. BTC(avg~221 BTC) 기준 values, ETH(avg~2821 ETH)는 volume 필드 13× 스케일. `list_strategies(market)` 파라미터 추가로 UI가 market 선택 변경 시 자동 재하이드레이션. (2) **TradeRecord.buy_timestamp/sell_timestamp** 추가: 6개 엔진 전부에서 `data[idx].candle.timestamp.to_rfc3339()` 기록. UI 테이블이 `Buy Idx/Sell Idx` 대신 `Buy Time/Sell Time` 표시. (3) **데이터 범위 표시**: 신규 `get_data_range` Tauri 커맨드 + `/api/market/range` Axum 엔드포인트. SQL `COUNT/MIN/MAX(timestamp)` 한 번에 조회. 시뮬 페이지 하단에 "Data: 55,205 bars · 2020-01-01 → 2026-04-20" 힌트 표시. Since/Until 비워두면 전체 범위(최초~현재) 자동 사용. (4) `decay_buy/decay_sell`이 유일하게 보이는 이유 — V0 엔진 설계상 `urgent_buy` 경로는 `price_change ≤ -2% AND volume ≥ urgent_threshold` 동시 충족 필요로 드물고, 대부분 `buy_ready → decay confirm` 경로 발동이 정상. 다양한 signal 원하면 risk 파라미터(trailing_stop_pct, fixed_stop_loss_pct, fixed_take_profit_pct, max_hold_periods) 활성화. 매뉴얼에 명시. 테스트 211개 통과 |
| 2026-04-23 | - | success | 최적화 Solutions 카드 위치 버그 수정 — 클릭한 행과 무관하게 뷰포트 정중앙에 모달이 뜨던 문제 해결. `fixed inset-0` 풀스크린 모달을 완전 제거하고 **인라인 확장 행** 패턴으로 전환: 테이블 map 블록을 `<Fragment>`로 감싸고 `selectedSolution === s` 비교로 해당 행 바로 아래에 `colSpan={ALL_METRICS.length + 3}`인 `<tr>` 확장. 같은 행 재클릭 시 접힘(toggle). 부수 정리: Modal 헤더의 메트릭 중복 표시 제거(이미 같은 행 위쪽에 있음), 중복 "Apply → Simulation" 버튼 중 Modal 쪽 삭제(Row 버튼 유지), `X` / `stopPropagation` 기반 닫기 로직 제거. 순 감소 약 45줄([OptimizationPage.tsx](src/pages/OptimizationPage.tsx)). Zustand `selectedSolution` state는 유지(페이지 내 UI state지만 별도 정리 분리). `npm run vite:build` 통과 |
| 2026-04-23 | - | success | V3.1 RegimeAdaptive (trade-value) 전략 추가 — V3 포팅 + 신호 기준을 `volume` → `close × volume` (KRW 거래대금) 으로 전환. V3 에서 하드코딩이던 4개 상수(`cutoff_tv_mult=1.0`, `urgent_sell_tv_mult=2.0`, `sell_ready_price_rise=1.0`, `sell_wait_max=168`)를 `TradingParameters.v31_*` 필드로 노출해 NSGA-II 탐색 가능. 총 43개 `v31_*` 필드 + 전략 struct + `StrategyRegistry` 등록 + `Optimizer::get/set_parameter` 확장 + 유닛 테스트 4개. 매뉴얼 [Manual/strategy-v3.1-trade-value.md](Manual/strategy-v3.1-trade-value.md) 신규. 프론트엔드는 `list_strategies` 동적 로딩이라 **수정 불필요** — Simulation/Optimization 페이지 셀렉터에 자동 노출 (SettingsPage 의 하드코딩 리스트는 "Default Strategy" 설정용이라 영향 없음). `cargo check --features tauri-app` + `cargo test --no-default-features` + `npm run vite:build` 전부 통과 |
| 2026-04-23 | - | success | 최적화 정수 파라미터 반올림 + `step` 필드 의미 정리 — NSGA-II `set_parameter`의 정수 캐스팅 5곳(`v3_min_hold_bars`, `v3_volume_lookback`, `v31_min_hold_bars`, `v31_volume_lookback`, `v31_sell_wait_max`)을 `value as i32`(truncation) → `value.round() as i32`로 변경. 근본 이유: 옵티마이저는 `[min, max]` 연속 공간에서 균등 섭동을 주는데 truncation은 평균적으로 -0.5의 음의 편향을 만들어 세대 누적 시 "정수 파라미터가 하한 쪽으로 drift"하는 현상이 생김. 반올림으로 편향 제거. 추가로 `ParameterRange.step` 필드는 옵티마이저 경로에서는 dead code(`Grep \.step` → 0건)지만 [SimulationPage.tsx:180](src/pages/SimulationPage.tsx#L180)의 HTML `<input step>` UI 힌트로 live code임을 확인 — 제거 대신 [models/config.rs:8-10](src-tauri/src/models/config.rs#L8-L10)에 "UI 전용 힌트, 옵티마이저는 무시" 주석으로 의미 좁힘(직렬화 스키마 호환성 유지). `cargo build --features tauri-app` 통과 |
| 2026-04-23 | - | success | 최적화 Recent Runs "Load" 로딩 인디케이터 추가 — 큰 run(최대 249k 행 / 5000세대)은 `get_optimization_run_history`가 행마다 `parameters`+`metrics_json` 2회 JSON 파싱 후 Tauri IPC로 직렬화 이동하므로 수 초~수십 초 대기 발생(구조상 정상). 사용자 피드백 반영해 시각 인디케이터 3단 추가: (1) Load 버튼을 클릭한 행에 `Loading...` 텍스트 + 3px 스피너 인라인 교체, 동시에 모든 Load 버튼 `disabled`로 중복 호출 차단([OptimizationPage.tsx:~625](src/pages/OptimizationPage.tsx)). (2) Recent Runs 카드 헤더 아래에 sticky 1px 프로그레스 바(`px-4 py-2` 밴드 + "Loading Run #N — deserializing…" 텍스트 + indeterminate 슬라이드 애니메이션) 노출([OptimizationPage.tsx:587-600](src/pages/OptimizationPage.tsx)). (3) `@keyframes indeterminateSlide` + `.animate-indeterminate` 유틸 CSS 추가([index.css:65-74](src/index.css#L65-L74)) — 1.2s 주기로 `translateX(-100%→400%)`. 상태는 Zustand `loadingRunId: number \| null`로 관리, `handleLoadRun` 시작 시 set, `finally`에서 null로 reset. `npm run vite:build` 통과 |
| 2026-04-23 | - | success | 최적화 Recent Runs "Load" 후 Apply → Simulation 전략 누락 버그 수정 — 근본 원인: `handleLoadRun`이 `genHistory`만 복원하고 `optimizationStore`의 `selectedStrategy`/`market`/`timeframe`/`since`/`until`을 건드리지 않아, 이후 `Apply → Simulation`이 폼의 stale한 dropdown 값을 `applyOptimizedParams`로 전달 → Simulation 탭에 run 원래 전략과 다른 값이 들어감. 해결: [OptimizationRunSummary](src-tauri/src/commands/optimization.rs#L447-L458) DTO에 `constraints: Option<String>` 추가 + `list_optimization_runs` SELECT에 `r.constraints` 포함(이미 insert 시 저장 중이던 JSON을 노출만). 프론트 `OptimizationRunSummary` 타입([lib/api.ts:148-158](src/lib/api.ts#L148-L158))에 동기화. [handleLoadRun](src/pages/OptimizationPage.tsx#L141-L168)를 `(run: OptimizationRunSummary)` 시그니처로 바꾸고 constraints JSON을 파싱해 `patchConfig({ selectedStrategy, market, timeframe, since, until })` 호출. `since`/`until`은 ISO string → `slice(0, 10)`로 폼 date 형식에 맞춤. `cargo build --features tauri-app` + `npm run vite:build` 통과 |
| 2026-04-28 | - | success | Live 화면 폭주의 2차 통로 차단 — 미실현 P/L을 가격 비율 기반으로 단순화. **상황**: 1차 수정으로 백엔드 `current_equity`는 정상화(+3.6%)됐으나 화면에는 여전히 18조 KRW로 표시. **DB 직접 확인**: `live_sessions.current_buy_volume = 22,078,119,896 ETH`(폭주값)으로 남아 있고, 프런트 [deriveSessionPnl](src/stores/liveTradingStore.ts#L184)이 `unrealized = (tick − buy) × buy_volume`으로 미실현을 계산하므로 `54,000 × 22e9 = 1.19e15`로 표시 단계에서 재폭주. 폭주의 출처는 V3 전략 내부 `last_set_volume`이 비현실적 값을 만드는 별개 버그. **수정**: 미실현을 `baseEquity × (tick / buy_price − 1)`으로 변경 — holding 자산 전체가 코인으로 변환된 것으로 보고 가격 비율만 적용. `current_buy_volume` 의존 제거 → 폭주값이 화면에 새는 통로 차단. 의미상 더 정확하고 buy_volume 컬럼·`last_set_volume` 저장 경로가 후속 정리 대상으로 분리됨. **검증**: `npx tsc --noEmit`에서 `liveTradingStore.ts` type-clean. (작업 트리에 별개 진행 중 작업의 TS 에러가 있으나 본 변경과 무관) |
| 2026-04-27 | - | success | Live paper sessions equity 폭주 수정 — bar 누적 → trade 기반 단순계산. **증상**: `Short_156%` 세션 equity 18.87조 KRW (+1,886,744,120%). **근본 원인**: [session_engine.rs:108](src-tauri/src/services/session_engine.rs#L108)이 `initial_capital × (1 + fee_adjusted_return/100)` 으로 계산하는데, `fee_adjusted_return` = `total_return` = `∏(1 + bar_return) − 1` (bar별 누적). 입력 hour 캔들 한 개의 비정상값(close≈0/timestamp 결손)이 `daily_return`을 폭주시키면 곱셈이 천만 배로 번짐. **수정**: equity 계산을 trade 기반으로 단일화 — `initial × ∏(1 + pnl_pct − 2·fee)` over closed trades. holding 중 미실현은 프런트엔드 `deriveSessionPnl`이 tick으로 처리 중이므로 책임 분리. **파급 정리**: (1) [session_engine.rs:108-114](src-tauri/src/services/session_engine.rs#L108-L114) equity 한 줄 → fold 교체. (2) `regime_adaptive.rs`/`regime_adaptive_v31.rs`/`enhanced_adaptive.rs`의 `result.fee_adjusted_return = result.total_return;` 복사 줄 삭제 (3곳). (3) `SimulationResult.fee_adjusted_return` 필드 제거 ([trading.rs:332](src-tauri/src/models/trading.rs#L332), Default impl 동시 정리). (4) 프런트 [types/index.ts:76](src/types/index.ts#L76) 동기화. **검증**: `cargo build --features tauri-app` 통과, `cargo test --test cum_return_parity_test` 5/5 통과, `npm run vite:build` 통과. (lib 의존 통합 테스트는 사전부터 있던 환경 DLL 이슈로 실행 불가 — 변경과 무관) |
| 2026-04-23 | - | success | 최적화 Recent Runs Load 전(全)세대 일괄 페치 → 세대별 lazy load로 전환 — **백엔드**: 기존 `get_optimization_run_history`(모든 행) + `get_optimization_run_results`(최신 1세대) 두 커맨드를 단일 `get_optimization_run_generation(run_id, generation: Option<i64>)` + `OptimizationGenerationView { generation, max_generation, solutions }` DTO로 대체([optimization.rs:508-619](src-tauri/src/commands/optimization.rs#L508-L619)). `generation=None`이면 MAX(gen)+그 세대만 반환. `lib.rs` invoke_handler 3줄 → 1줄. **프론트 스토어**: `loadGenHistory(snapshots[])` 제거 → `loadRunLatest(view)` + `upsertGeneration(view)` 분리([optimizationStore.ts](src/stores/optimizationStore.ts)). 새 필드 `maxGeneration`(슬라이더 상한), `scrubLoading`(fetch 진행 중). `upsertGeneration`에 race-safe 가드(`state.currentRunId !== runId` 드롭). **페이지**: `handleLoadRun`은 `getOptimizationRunGeneration(runId, null)` 1회만 호출 → 초기 응답 수초→수백ms. 슬라이더 스크럽 시 180ms 디바운스 `useEffect`로 미캐시 세대를 백그라운드 fetch([OptimizationPage.tsx](src/pages/OptimizationPage.tsx)). 슬라이더 `firstGen=1` 고정, `scrubMax = maxGeneration ?? progress.total_generations` 분기로 로드/라이브 양쪽 정상 동작. "fetching Gen N…" 인라인 스피너 추가. `cargo build --features tauri-app` + `cargo test --lib` 49 pass + `npm run vite:build` 통과 |
