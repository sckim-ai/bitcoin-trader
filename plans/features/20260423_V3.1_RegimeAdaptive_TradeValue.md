# V3.1 RegimeAdaptive (거래대금 기반) Implementation Plan

> **For agentic workers:** Steps use checkbox (`- [ ]`) syntax for tracking. Execute task-by-task and verify each step before moving on.

## TL;DR

**Quick Summary:** V3 RegimeAdaptive를 포팅하여 **거래량(BTC 단위)** 대신 **거래대금(KRW = close × volume)**을 신호 기준으로 사용하는 V3.1 전략 추가. 동시에 V3의 legacy 하드코딩 상수 4개(`cutoff_volume_multiplier=1.0`, `urgent_sell vol×2`, `sell_wait_max=168`, `sell_ready_price_rise=1.0`)를 파라미터화하여 옵티마이저 탐색공간으로 노출.

**Deliverables:**
- 신규 파일: `src-tauri/src/strategies/regime_adaptive_v31.rs`
- 신규 파일: `Manual/strategy-v3.1-trade-value.md`
- 수정: `src-tauri/src/models/trading.rs` (v31_* 필드 +4 상수)
- 수정: `src-tauri/src/strategies/mod.rs` (등록)
- 수정: `src-tauri/src/core/optimizer.rs` (get/set_parameter 확장)
- 수정: `src-tauri/tests/optimizer_test.rs` 또는 신규 `tests/v31_strategy_test.rs`

**Estimated Effort:** 2–3시간 (구현 1시간, 테스트 1시간, 매뉴얼/빌드 확인 30분)

**Critical Path:** Task 1 → Task 2 → Task 3 → Task 4 → Task 5 (병렬화 불가, 각 단계가 다음 단계 컴파일 의존)

**Goal:** ETH 전용 트레이딩 환경에서 가격 스케일 변화에 둔감한 거래대금 기반 신호를 가진 V3 변형 전략을 제공하고, legacy 하드코딩 제약에서 벗어나 4개 상수를 최적화 대상으로 승격.

**Architecture:**
V3와 **독립된 새 전략**. `TradingParameters` struct는 공유하되 `v31_*` 접두어로 네임스페이스 분리(V3/V3.1 동시 최적화 가능, DB 결과 분리). V3 원본은 **손대지 않음** (verbatim parity 유지). 신호 기준 수식만 `vol → close*volume`로 교체, `set_volume` 상태변수는 `set_trade_value`로 의미만 변경.

**Tech Stack:** Rust (Tauri backend), 기존 `Strategy` trait, NSGA-II optimizer, serde.

---

## File Structure

| 파일 | 작업 | 역할 |
|------|------|------|
| `src-tauri/src/strategies/regime_adaptive_v31.rs` | 신규 | V3.1 전략 본체 — `Strategy` trait 구현 |
| `src-tauri/src/models/trading.rs` | 수정 | `TradingParameters`에 `v31_*` 필드 41개 추가 + 기본값 |
| `src-tauri/src/strategies/mod.rs` | 수정 | `pub mod regime_adaptive_v31;` + registry 등록 |
| `src-tauri/src/core/optimizer.rs` | 수정 | `get_parameter`/`set_parameter` match 에 `v31_*` 아암 추가 |
| `src-tauri/tests/v31_strategy_test.rs` | 신규 | V3.1 등록·실행·거래대금 감지 유닛 테스트 |
| `Manual/strategy-v3.1-trade-value.md` | 신규 | V3.1 매뉴얼 (프로젝트 규칙) |

**디자인 경계:**
- V3와 V3.1은 별도 파일 — `Strategy` trait 다형성으로 분리. 공통 코드(`rsi_param`, `safe_rsi`) 중복 허용 (Rule of Three: 2곳이면 중복 아님).
- 파라미터 필드는 접두어만 다른 완전 복제 + 4개 추가. `Default` 값은 ETH 거래대금 스케일로 초기화.

---

## Hardcoded → Parameter 매핑

V3에서 V3.1로 옮기며 파라미터화되는 4개 상수:

| V3 원본 ([regime_adaptive.rs](../../src-tauri/src/strategies/regime_adaptive.rs)) | V3.1 파라미터 | 기본값 | 최적화 범위 |
|--------|-----------|--------|-------------|
| `vol > set_volume * 1.0` (line 242) | `v31_cutoff_tv_mult` | 1.0 | 0.5 – 3.0 |
| `vol > sell_ready_vol * 2.0` (line 247) | `v31_urgent_sell_tv_mult` | 2.0 | 1.0 – 5.0 |
| `prev_close * 1.0 < cls` (line 261) | `v31_sell_ready_price_rise` | 1.0 | 0.99 – 1.05 |
| `if sell_po < 168` (line 271) | `v31_sell_wait_max` | 168 | 24 – 720 (i32, step 24) |

---

## Task 1: `TradingParameters`에 V3.1 필드 추가

**Files:**
- Modify: `src-tauri/src/models/trading.rs`

**Goal:** 41개 `v31_*` 필드 추가 (V3의 37개 필드 + 4개 신규 상수). serde default 함수로 역직렬화 호환성 확보(기존 DB 레코드에 v31 필드 없어도 로드 가능).

- [ ] **Step 1: `TradingParameters` struct에 v31_* 필드 추가**

[src-tauri/src/models/trading.rs](src-tauri/src/models/trading.rs) 의 V5 필드 블록 직후, `}` 직전에 다음 블록 삽입:

```rust
    // ─── V3.1 RegimeAdaptive (trade-value based) ───
    // 신호 기준: vol*close (KRW 거래대금). V3의 BTC 거래량 기반에서 전환.
    // V3의 모든 v3_* 필드와 동형이지만 스케일이 다름 (KRW 단위).
    #[serde(default = "d_v31_ubv_lo")] pub v31_urgent_buy_tv_lo: f64,
    #[serde(default = "d_v31_ubv_hi")] pub v31_urgent_buy_tv_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_urgent_buy_tv_pow: f64,
    #[serde(default = "d_v31_bv_lo")]  pub v31_buy_tv_lo: f64,
    #[serde(default = "d_v31_bv_hi")]  pub v31_buy_tv_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_buy_tv_pow: f64,
    #[serde(default = "d_v31_bpd_lo")] pub v31_buy_price_drop_lo: f64,
    #[serde(default = "d_v31_bpd_hi")] pub v31_buy_price_drop_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_buy_price_drop_pow: f64,
    #[serde(default = "d_v31_bdc_lo")] pub v31_buy_decay_lo: f64,
    #[serde(default = "d_v31_bdc_hi")] pub v31_buy_decay_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_buy_decay_pow: f64,
    #[serde(default = "d_v31_bps_lo")] pub v31_buy_psy_lo: f64,
    #[serde(default = "d_v31_bps_hi")] pub v31_buy_psy_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_buy_psy_pow: f64,
    #[serde(default = "d_v31_bw_lo")]  pub v31_buy_wait_lo: f64,
    #[serde(default = "d_v31_bw_hi")]  pub v31_buy_wait_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_buy_wait_pow: f64,

    #[serde(default = "d_v31_ssl_lo")] pub v31_sell_stop_loss_lo: f64,
    #[serde(default = "d_v31_ssl_hi")] pub v31_sell_stop_loss_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_sell_stop_loss_pow: f64,
    #[serde(default = "d_v31_spf_lo")] pub v31_sell_profit_lo: f64,
    #[serde(default = "d_v31_spf_hi")] pub v31_sell_profit_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_sell_profit_pow: f64,
    #[serde(default = "d_v31_sv_lo")]  pub v31_sell_tv_lo: f64,
    #[serde(default = "d_v31_sv_hi")]  pub v31_sell_tv_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_sell_tv_pow: f64,
    #[serde(default = "d_v31_sdc_lo")] pub v31_sell_decay_lo: f64,
    #[serde(default = "d_v31_sdc_hi")] pub v31_sell_decay_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_sell_decay_pow: f64,
    #[serde(default = "d_v31_sfx_lo")] pub v31_sell_fixed_sl_lo: f64,
    #[serde(default = "d_v31_sfx_hi")] pub v31_sell_fixed_sl_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_sell_fixed_sl_pow: f64,
    #[serde(default = "d_v31_smh_lo")] pub v31_sell_max_hold_lo: f64,
    #[serde(default = "d_v31_smh_hi")] pub v31_sell_max_hold_hi: f64,
    #[serde(default = "d_v31_pow")]    pub v31_sell_max_hold_pow: f64,

    #[serde(default = "d_v31_fee")]            pub v31_fee_rate: f64,
    #[serde(default = "d_v31_min_hold")]       pub v31_min_hold_bars: i32,
    #[serde(default = "d_v31_lookback")]       pub v31_volume_lookback: i32,

    // 신규 파라미터화된 4개 상수 (V3에서 하드코딩이었음)
    #[serde(default = "d_v31_cutoff_mult")]        pub v31_cutoff_tv_mult: f64,
    #[serde(default = "d_v31_urg_sell_mult")]      pub v31_urgent_sell_tv_mult: f64,
    #[serde(default = "d_v31_sell_ready_rise")]    pub v31_sell_ready_price_rise: f64,
    #[serde(default = "d_v31_sell_wait_max")]      pub v31_sell_wait_max: i32,
```

- [ ] **Step 2: default 함수들 추가**

파일 하단의 V5 default 함수 블록 바로 뒤에 삽입. ETH 스케일(hourly trade value 1e9 ~ 1e11 KRW 수준) 기준:

```rust
// V3.1 defaults — ETH/hour trade value (KRW) 스케일 기준.
// V3 BTC volume 기본값에 대략 4_000_000 (ETH 가격) 을 곱한 수준으로 시작.
fn d_v31_ubv_lo() -> f64 { 8.4e10 }   // 21000 BTC-vol * 4M
fn d_v31_ubv_hi() -> f64 { 3.0e11 }   // 75000 * 4M
fn d_v31_bv_lo()  -> f64 { 2.0e10 }
fn d_v31_bv_hi()  -> f64 { 7.0e10 }
fn d_v31_bpd_lo() -> f64 { 1.045 }
fn d_v31_bpd_hi() -> f64 { 1.025 }
fn d_v31_bdc_lo() -> f64 { 0.09 }
fn d_v31_bdc_hi() -> f64 { 0.077 }
fn d_v31_bps_lo() -> f64 { 0.14 }
fn d_v31_bps_hi() -> f64 { -0.24 }
fn d_v31_bw_lo()  -> f64 { 492.0 }
fn d_v31_bw_hi()  -> f64 { 336.0 }
fn d_v31_ssl_lo() -> f64 { 0.85 }
fn d_v31_ssl_hi() -> f64 { 0.82 }
fn d_v31_spf_lo() -> f64 { 1.145 }
fn d_v31_spf_hi() -> f64 { 1.09 }
fn d_v31_sv_lo()  -> f64 { 8.0e9 }
fn d_v31_sv_hi()  -> f64 { 1.26e11 }
fn d_v31_sdc_lo() -> f64 { 0.116 }
fn d_v31_sdc_hi() -> f64 { 0.079 }
fn d_v31_sfx_lo() -> f64 { 0.08 }
fn d_v31_sfx_hi() -> f64 { 0.08 }
fn d_v31_smh_lo() -> f64 { 672.0 }
fn d_v31_smh_hi() -> f64 { 816.0 }
fn d_v31_pow()    -> f64 { 2.0 }
fn d_v31_fee()    -> f64 { 0.0005 }
fn d_v31_min_hold() -> i32 { 21 }
fn d_v31_lookback() -> i32 { 35 }
fn d_v31_cutoff_mult()     -> f64 { 1.0 }
fn d_v31_urg_sell_mult()   -> f64 { 2.0 }
fn d_v31_sell_ready_rise() -> f64 { 1.0 }
fn d_v31_sell_wait_max()   -> i32 { 168 }
```

- [ ] **Step 3: `Default` impl 블록에 v31 필드 초기화 추가**

기존 `impl Default for TradingParameters` 의 V5 초기화 라인 뒤, `}` 직전에 삽입:

```rust
            v31_urgent_buy_tv_lo: d_v31_ubv_lo(),
            v31_urgent_buy_tv_hi: d_v31_ubv_hi(),
            v31_urgent_buy_tv_pow: d_v31_pow(),
            v31_buy_tv_lo: d_v31_bv_lo(),
            v31_buy_tv_hi: d_v31_bv_hi(),
            v31_buy_tv_pow: d_v31_pow(),
            v31_buy_price_drop_lo: d_v31_bpd_lo(),
            v31_buy_price_drop_hi: d_v31_bpd_hi(),
            v31_buy_price_drop_pow: d_v31_pow(),
            v31_buy_decay_lo: d_v31_bdc_lo(),
            v31_buy_decay_hi: d_v31_bdc_hi(),
            v31_buy_decay_pow: d_v31_pow(),
            v31_buy_psy_lo: d_v31_bps_lo(),
            v31_buy_psy_hi: d_v31_bps_hi(),
            v31_buy_psy_pow: d_v31_pow(),
            v31_buy_wait_lo: d_v31_bw_lo(),
            v31_buy_wait_hi: d_v31_bw_hi(),
            v31_buy_wait_pow: d_v31_pow(),
            v31_sell_stop_loss_lo: d_v31_ssl_lo(),
            v31_sell_stop_loss_hi: d_v31_ssl_hi(),
            v31_sell_stop_loss_pow: d_v31_pow(),
            v31_sell_profit_lo: d_v31_spf_lo(),
            v31_sell_profit_hi: d_v31_spf_hi(),
            v31_sell_profit_pow: d_v31_pow(),
            v31_sell_tv_lo: d_v31_sv_lo(),
            v31_sell_tv_hi: d_v31_sv_hi(),
            v31_sell_tv_pow: d_v31_pow(),
            v31_sell_decay_lo: d_v31_sdc_lo(),
            v31_sell_decay_hi: d_v31_sdc_hi(),
            v31_sell_decay_pow: d_v31_pow(),
            v31_sell_fixed_sl_lo: d_v31_sfx_lo(),
            v31_sell_fixed_sl_hi: d_v31_sfx_hi(),
            v31_sell_fixed_sl_pow: d_v31_pow(),
            v31_sell_max_hold_lo: d_v31_smh_lo(),
            v31_sell_max_hold_hi: d_v31_smh_hi(),
            v31_sell_max_hold_pow: d_v31_pow(),
            v31_fee_rate: d_v31_fee(),
            v31_min_hold_bars: d_v31_min_hold(),
            v31_volume_lookback: d_v31_lookback(),
            v31_cutoff_tv_mult: d_v31_cutoff_mult(),
            v31_urgent_sell_tv_mult: d_v31_urg_sell_mult(),
            v31_sell_ready_price_rise: d_v31_sell_ready_rise(),
            v31_sell_wait_max: d_v31_sell_wait_max(),
```

- [ ] **Step 4: `cargo build` 로 컴파일 확인**

Run: `cd src-tauri && cargo build --features tauri-app`
Expected: `Compiling bitcoin_trader_lib`, 성공. 경고만 허용, 에러 0.

- [ ] **Step 5: 커밋**

```bash
git add src-tauri/src/models/trading.rs
git commit -m "feat(v31): add V3.1 parameter fields to TradingParameters"
```

---

## Task 2: V3.1 Strategy 구현

**Files:**
- Create: `src-tauri/src/strategies/regime_adaptive_v31.rs`
- Modify: `src-tauri/src/strategies/mod.rs`

**Goal:** [regime_adaptive.rs](../../src-tauri/src/strategies/regime_adaptive.rs)를 복제 후 다음 치환 적용:
1. `current.candle.volume` → `current.candle.close * current.candle.volume` (새 로컬 변수 `tv` 도입)
2. `set_volume` 상태변수 → `set_trade_value` (의미 명확화, 타입 동일 f64)
3. 4개 하드코딩 상수를 파라미터로 교체
4. 모든 파라미터 참조를 `params.v3_*` → `params.v31_*`
5. `name()` / `description()` 변경, 트레이스 env는 `V31_TRACE_PATH`로 격리

- [ ] **Step 1: 새 파일 `regime_adaptive_v31.rs` 작성**

Create `src-tauri/src/strategies/regime_adaptive_v31.rs`:

```rust
use crate::core::engine::push_signal;
use crate::core::signals::{PositionState, SignalType, TradingSignal};
use crate::models::config::ParameterRange;
use crate::models::market::MarketData;
use crate::models::trading::{SignalEvent, SimulationResult, TradeRecord, TradingParameters};
use std::collections::HashMap;

use super::Strategy;

/// V3.1 RegimeAdaptive (trade-value edition) — V3 포팅 버전.
/// V3 대비 차이:
///   1. 신호 기준이 **거래대금(close × volume, KRW)**. V3는 BTC 거래량.
///      → 가격 스케일 변화(예: ETH 2M → 5M)에 강건.
///   2. V3의 하드코딩 상수 4개가 파라미터화됨:
///        * `v31_cutoff_tv_mult` (V3: 1.0)
///        * `v31_urgent_sell_tv_mult` (V3: 2.0)
///        * `v31_sell_ready_price_rise` (V3: 1.0)
///        * `v31_sell_wait_max` (V3: 168)
///   3. `set_volume` → `set_trade_value` 로 이름만 변경 (의미상 거래대금).
/// 나머지 로직(RSI 보간, bar-by-bar returns, state machine)은 V3와 동일.
pub struct RegimeAdaptiveV31Strategy;

fn rsi_param(rsi: f64, lo: f64, hi: f64, pow: f64) -> f64 {
    let t = ((rsi - 20.0) / 60.0).clamp(0.0, 1.0);
    let t_pow = if pow > 0.0 { t.powf(pow) } else { t };
    lo + (hi - lo) * t_pow
}

fn safe_rsi(rsi: f64) -> f64 {
    if rsi > 0.0 { rsi } else { 50.0 }
}

impl Strategy for RegimeAdaptiveV31Strategy {
    fn name(&self) -> &str {
        "Regime Adaptive TV (V3.1)"
    }

    fn description(&self) -> &str {
        "V3의 거래대금(KRW) 기반 버전 — 가격 스케일 독립성 + legacy 하드코딩 4개 파라미터화"
    }

    fn run_simulation(&self, data: &[MarketData], params: &TradingParameters) -> SimulationResult {
        let mut result = SimulationResult::default();
        if data.len() < 30 {
            return result;
        }

        let mut position: i32 = 0;
        let mut previous_position: i32 = 0;
        let mut buy_price: f64 = 0.0;
        let mut set_trade_value: f64 = 0.0;
        let mut buy_sign: i32 = 0;
        let mut sell_sign: i32 = 0;
        let mut buy_po: i32 = 0;
        let mut sell_po: i32 = 0;
        let mut highest_since_buy: f64 = 0.0;
        let mut hold_bars: i32 = 0;
        let mut entry_rsi: f64 = 50.0;

        let mut market_returns: Vec<f64> = Vec::new();
        let mut strategy_returns: Vec<f64> = Vec::new();

        let mut buy_index: usize = 0;
        let mut buy_signal_str = String::new();
        let mut wins: usize = 0;
        let mut sum_wins: f64 = 0.0;
        let mut sum_losses: f64 = 0.0;
        let mut consecutive_losses: usize = 0;
        let mut max_consecutive_losses: usize = 0;
        let mut trade_returns: Vec<f64> = Vec::new();

        let mut prev_signal_type = String::from("ready");

        let trace_path = std::env::var("V31_TRACE_PATH").ok();
        let mut trace_sink: Option<std::fs::File> = trace_path.as_ref().map(|p| {
            let mut f = std::fs::File::create(p).expect("trace file");
            use std::io::Write as _;
            writeln!(f, "bar,timestamp,close,tv,rsi,psyDay,pos,bSign,sSign,buyPo,sellPo,setTv,entryRsi,event").unwrap();
            f
        });

        for i in 1..data.len() {
            let current = &data[i];
            let previous = &data[i - 1];
            let current_rsi = safe_rsi(current.indicators.rsi);
            let mut skip_exec = false;

            // 거래대금 (KRW) — V3.1 핵심 변경점
            let tv = current.candle.close * current.candle.volume;

            if position == 0 {
                let urgent_buy_tv = rsi_param(
                    current_rsi,
                    params.v31_urgent_buy_tv_lo,
                    params.v31_urgent_buy_tv_hi,
                    params.v31_urgent_buy_tv_pow,
                );
                let buy_ready_tv = rsi_param(
                    current_rsi,
                    params.v31_buy_tv_lo,
                    params.v31_buy_tv_hi,
                    params.v31_buy_tv_pow,
                );
                let buy_price_drop = rsi_param(
                    current_rsi,
                    params.v31_buy_price_drop_lo,
                    params.v31_buy_price_drop_hi,
                    params.v31_buy_price_drop_pow,
                );
                let buy_decay = rsi_param(
                    current_rsi,
                    params.v31_buy_decay_lo,
                    params.v31_buy_decay_hi,
                    params.v31_buy_decay_pow,
                );
                let buy_psy = rsi_param(
                    current_rsi,
                    params.v31_buy_psy_lo,
                    params.v31_buy_psy_hi,
                    params.v31_buy_psy_pow,
                );
                let buy_wait_max = rsi_param(
                    current_rsi,
                    params.v31_buy_wait_lo,
                    params.v31_buy_wait_hi,
                    params.v31_buy_wait_pow,
                ) as i32;

                let cls = current.candle.close;
                let prev_close = previous.candle.close;
                let psy = current.indicators.psy_day;

                if tv > urgent_buy_tv && prev_close * buy_price_drop > cls {
                    buy_sign = 3;
                    buy_signal_str = "regime_urgent_buy_tv".into();
                    result.buy_signals += 1;
                } else {
                    if tv > buy_ready_tv && prev_close * buy_price_drop > cls {
                        buy_sign = 1;
                        buy_po = 0;
                        set_trade_value = tv;
                        result.buy_signals += 1;
                        skip_exec = true;
                    } else if buy_sign == 1 {
                        buy_po += 1;
                        if buy_po < buy_wait_max && tv > buy_ready_tv {
                            buy_po = 0;
                        } else if tv < set_trade_value * buy_decay && psy < buy_psy {
                            buy_sign = 2;
                            buy_signal_str = "regime_decay_buy_tv".into();
                        }
                    }
                }

                if !skip_exec && (buy_sign == 2 || buy_sign == 3) {
                    buy_price = cls;
                    position = 1;
                    buy_sign = 0;
                    highest_since_buy = cls;
                    hold_bars = 0;
                    entry_rsi = current_rsi;
                    buy_index = i;
                }
            } else if position == 1 {
                hold_bars += 1;
                if current.candle.close > highest_since_buy {
                    highest_since_buy = current.candle.close;
                }

                if hold_bars < params.v31_min_hold_bars {
                    // goto CalculateReturns
                } else {
                    let fixed_sl = rsi_param(
                        entry_rsi,
                        params.v31_sell_fixed_sl_lo,
                        params.v31_sell_fixed_sl_hi,
                        params.v31_sell_fixed_sl_pow,
                    );
                    let stop_loss_price = rsi_param(
                        entry_rsi,
                        params.v31_sell_stop_loss_lo,
                        params.v31_sell_stop_loss_hi,
                        params.v31_sell_stop_loss_pow,
                    );
                    let sell_profit = rsi_param(
                        entry_rsi,
                        params.v31_sell_profit_lo,
                        params.v31_sell_profit_hi,
                        params.v31_sell_profit_pow,
                    );
                    let sell_ready_tv = rsi_param(
                        entry_rsi,
                        params.v31_sell_tv_lo,
                        params.v31_sell_tv_hi,
                        params.v31_sell_tv_pow,
                    );
                    let sell_decay = rsi_param(
                        entry_rsi,
                        params.v31_sell_decay_lo,
                        params.v31_sell_decay_hi,
                        params.v31_sell_decay_pow,
                    );
                    let max_hold = rsi_param(
                        entry_rsi,
                        params.v31_sell_max_hold_lo,
                        params.v31_sell_max_hold_hi,
                        params.v31_sell_max_hold_pow,
                    ) as i32;

                    let cls = current.candle.close;
                    let prev_close = previous.candle.close;

                    // 1. Fixed stop loss
                    if fixed_sl > 0.0 && cls < buy_price * (1.0 - fixed_sl) {
                        sell_sign = 2;
                    }

                    // 2. Max hold
                    if max_hold > 0 && hold_bars > max_hold {
                        sell_sign = 2;
                    }

                    // 3. TV cutoff — v31_cutoff_tv_mult (V3: hardcoded 1.0)
                    if tv > set_trade_value * params.v31_cutoff_tv_mult
                        && cls < buy_price * stop_loss_price
                    {
                        sell_sign = 2;
                    }

                    // 4. Urgent sell trigger — v31_urgent_sell_tv_mult (V3: hardcoded 2.0)
                    if tv > sell_ready_tv * params.v31_urgent_sell_tv_mult
                        && cls > buy_price * sell_profit
                    {
                        sell_sign = 3;
                        skip_exec = true;
                    }

                    if !skip_exec {
                        if sell_sign == 3 && tv > sell_ready_tv {
                            sell_sign = 2;
                        }

                        // 6. Ready sell — v31_sell_ready_price_rise (V3: hardcoded 1.0)
                        if sell_sign == 0
                            && tv > sell_ready_tv
                            && prev_close * params.v31_sell_ready_price_rise < cls
                        {
                            sell_sign = 1;
                            sell_po = 0;
                            skip_exec = true;
                        }

                        // 7. Decay confirm — v31_sell_wait_max (V3: hardcoded 168)
                        if !skip_exec && sell_sign == 1 {
                            sell_po += 1;
                            if sell_po < params.v31_sell_wait_max && tv > sell_ready_tv {
                                sell_po = 0;
                            } else if tv < set_trade_value * sell_decay {
                                sell_sign = 2;
                            }
                        }
                    }

                    if !skip_exec && sell_sign == 2 {
                        let sell_price = cls;
                        let pnl_pct = (sell_price - buy_price) / buy_price;
                        trade_returns.push(pnl_pct);
                        if pnl_pct > 0.0 {
                            wins += 1;
                            sum_wins += pnl_pct;
                            consecutive_losses = 0;
                        } else {
                            sum_losses += pnl_pct.abs();
                            consecutive_losses += 1;
                            if consecutive_losses > max_consecutive_losses {
                                max_consecutive_losses = consecutive_losses;
                            }
                        }
                        result.trades.push(TradeRecord {
                            buy_index,
                            sell_index: i,
                            buy_price,
                            sell_price,
                            pnl_pct,
                            hold_bars,
                            buy_signal: buy_signal_str.clone(),
                            sell_signal: "regime_sell_tv".into(),
                            buy_timestamp: data[buy_index].candle.timestamp.to_rfc3339(),
                            sell_timestamp: current.candle.timestamp.to_rfc3339(),
                        });
                        result.sell_signals += 1;

                        position = 0;
                        sell_sign = 0;
                        highest_since_buy = 0.0;
                        hold_bars = 0;
                    }
                }
            }

            // bar-by-bar compounding (V3 identical)
            if i > 10 {
                let daily_return = (current.candle.close - previous.candle.close)
                    / previous.candle.close;
                market_returns.push(daily_return);

                let mut bar_return = previous_position as f64 * daily_return;
                if previous_position == 0 && position == 1 {
                    bar_return -= params.v31_fee_rate;
                }
                if previous_position == 1 && position == 0 {
                    bar_return -= params.v31_fee_rate;
                }
                strategy_returns.push(bar_return);
            }

            push_signal(
                &mut result.signal_log,
                &mut prev_signal_type,
                i,
                data,
                position,
                buy_sign,
                sell_sign,
                previous_position,
            );

            if let Some(f) = trace_sink.as_mut() {
                let evt = if position == 1 && previous_position == 0 { "buy" }
                          else if position == 0 && previous_position == 1 { "sell" }
                          else { "" };
                let kst = current.candle.timestamp + chrono::Duration::hours(9);
                use std::io::Write as _;
                writeln!(
                    f,
                    "{},{},{},{:.2},{:.6},{:.6},{},{},{},{},{},{:.2},{:.6},{}",
                    i,
                    kst.format("%Y-%m-%dT%H:%M:%S"),
                    current.candle.close,
                    tv,
                    safe_rsi(current.indicators.rsi),
                    current.indicators.psy_day,
                    position, buy_sign, sell_sign, buy_po, sell_po,
                    set_trade_value, entry_rsi, evt
                ).unwrap();
            }

            previous_position = position;
        }

        // Performance metrics (V3 identical)
        let cumulative_product: f64 = strategy_returns.iter().map(|r| 1.0 + r).product();
        let total_return = cumulative_product - 1.0;
        let market_product: f64 = market_returns.iter().map(|r| 1.0 + r).product();
        let market_return = market_product - 1.0;

        let days = {
            let first = data[0].candle.timestamp;
            let last = data[data.len() - 1].candle.timestamp;
            let d = (last - first).num_seconds() as f64 / 86400.0;
            if d <= 0.0 { 1.0 } else { d }
        };
        let annual_return = (1.0 + total_return).powf(365.0 / days) - 1.0;

        let sharpe = if strategy_returns.len() > 1 {
            let mean: f64 = strategy_returns.iter().sum::<f64>() / strategy_returns.len() as f64;
            let variance = strategy_returns.iter().map(|r| (r - mean).powi(2)).sum::<f64>()
                / strategy_returns.len() as f64;
            let std = variance.sqrt();
            if std > 0.0 { mean / std * (365.0_f64 * 24.0).sqrt() } else { 0.0 }
        } else { 0.0 };

        let downside: Vec<f64> = strategy_returns.iter().copied().filter(|r| *r < 0.0).collect();
        let sortino = if downside.len() > 1 {
            let mean: f64 = strategy_returns.iter().sum::<f64>() / strategy_returns.len() as f64;
            let downside_std = (downside.iter().map(|r| r * r).sum::<f64>() / downside.len() as f64).sqrt();
            if downside_std > 0.0 { mean / downside_std * (365.0_f64 * 24.0).sqrt() } else { 0.0 }
        } else { 0.0 };

        let mut peak = 1.0_f64;
        let mut max_dd = 0.0_f64;
        let mut cum = 1.0_f64;
        for r in &strategy_returns {
            cum *= 1.0 + r;
            if cum > peak { peak = cum; }
            let dd = (peak - cum) / peak;
            if dd > max_dd { max_dd = dd; }
        }

        let total_trades = result.trades.len();
        result.total_trades = total_trades;
        result.total_return = total_return * 100.0;
        result.fee_adjusted_return = result.total_return;
        result.market_return = market_return * 100.0;
        result.annual_return = annual_return * 100.0;
        result.max_drawdown = max_dd * 100.0;
        result.max_consecutive_losses = max_consecutive_losses;
        if total_trades > 0 {
            result.win_rate = wins as f64 / total_trades as f64 * 100.0;
            result.avg_trade_return = trade_returns.iter().sum::<f64>() / total_trades as f64 * 100.0;
        }
        result.profit_factor = if sum_losses > 0.0 { sum_wins / sum_losses }
            else if sum_wins > 0.0 { f64::INFINITY } else { 0.0 };
        result.sharpe_ratio = sharpe;
        result.sortino_ratio = sortino;
        result.calmar_ratio = if max_dd > 0.0 { annual_return / max_dd } else { 0.0 };

        result.last_position = position;
        result.last_buy_price = buy_price;
        result.last_set_volume = set_trade_value;  // struct 필드명은 공용 (의미만 거래대금)
        result.last_hold_bars = hold_bars;
        result.last_entry_rsi = entry_rsi;
        result.last_highest_since_buy = highest_since_buy;
        result.last_signal_type = if position == 1 { "holding".into() } else { "idle".into() };

        let _ = buy_index;
        let _ = buy_signal_str;
        let _: Vec<SignalEvent> = result.signal_log.clone();

        result
    }

    fn get_latest_signal(
        &self,
        data: &[MarketData],
        params: &TradingParameters,
        position: &PositionState,
    ) -> TradingSignal {
        if data.len() < 3 {
            return TradingSignal {
                signal_type: SignalType::Hold,
                confidence: None,
                metadata: HashMap::new(),
            };
        }
        let prev = self.run_simulation(&data[..data.len() - 1], params);
        let full = self.run_simulation(data, params);

        let signal_type = if position.position == 0 {
            if full.last_position == 1 && prev.last_position == 0 {
                SignalType::Buy
            } else if full.total_trades > prev.total_trades && full.last_position == 1 {
                SignalType::Buy
            } else {
                SignalType::Hold
            }
        } else if full.total_trades > prev.total_trades && full.last_position == 0 {
            SignalType::Sell
        } else {
            SignalType::Hold
        };

        TradingSignal { signal_type, confidence: None, metadata: HashMap::new() }
    }

    fn parameter_ranges(&self) -> Vec<ParameterRange> {
        // ETH/hour trade value (KRW) 기준 탐색 범위.
        // 거래대금 lo/hi: 1e9 ~ 5e11, step 1e9 / 5e9.
        vec![
            ParameterRange { name: "v31_urgent_buy_tv_lo".into(), min: 1.0e10, max: 2.0e11, step: 5.0e9 },
            ParameterRange { name: "v31_urgent_buy_tv_hi".into(), min: 5.0e10, max: 5.0e11, step: 1.0e10 },
            ParameterRange { name: "v31_urgent_buy_tv_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_buy_tv_lo".into(), min: 5.0e9, max: 1.0e11, step: 1.0e9 },
            ParameterRange { name: "v31_buy_tv_hi".into(), min: 1.0e10, max: 2.0e11, step: 5.0e9 },
            ParameterRange { name: "v31_buy_tv_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_buy_price_drop_lo".into(), min: 0.9, max: 1.1, step: 0.005 },
            ParameterRange { name: "v31_buy_price_drop_hi".into(), min: 0.9, max: 1.1, step: 0.005 },
            ParameterRange { name: "v31_buy_price_drop_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_buy_decay_lo".into(), min: 0.01, max: 0.5, step: 0.005 },
            ParameterRange { name: "v31_buy_decay_hi".into(), min: 0.01, max: 0.5, step: 0.005 },
            ParameterRange { name: "v31_buy_decay_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_buy_psy_lo".into(), min: -1.0, max: 1.0, step: 0.02 },
            ParameterRange { name: "v31_buy_psy_hi".into(), min: -1.0, max: 1.0, step: 0.02 },
            ParameterRange { name: "v31_buy_psy_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_buy_wait_lo".into(), min: 10.0, max: 1000.0, step: 10.0 },
            ParameterRange { name: "v31_buy_wait_hi".into(), min: 10.0, max: 1000.0, step: 10.0 },
            ParameterRange { name: "v31_buy_wait_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_sell_stop_loss_lo".into(), min: 0.5, max: 1.0, step: 0.005 },
            ParameterRange { name: "v31_sell_stop_loss_hi".into(), min: 0.5, max: 1.0, step: 0.005 },
            ParameterRange { name: "v31_sell_stop_loss_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_sell_profit_lo".into(), min: 1.0, max: 2.0, step: 0.005 },
            ParameterRange { name: "v31_sell_profit_hi".into(), min: 1.0, max: 2.0, step: 0.005 },
            ParameterRange { name: "v31_sell_profit_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_sell_tv_lo".into(), min: 5.0e9, max: 1.0e11, step: 1.0e9 },
            ParameterRange { name: "v31_sell_tv_hi".into(), min: 1.0e10, max: 5.0e11, step: 5.0e9 },
            ParameterRange { name: "v31_sell_tv_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_sell_decay_lo".into(), min: 0.01, max: 0.5, step: 0.005 },
            ParameterRange { name: "v31_sell_decay_hi".into(), min: 0.01, max: 0.5, step: 0.005 },
            ParameterRange { name: "v31_sell_decay_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_sell_fixed_sl_lo".into(), min: 0.01, max: 0.3, step: 0.005 },
            ParameterRange { name: "v31_sell_fixed_sl_hi".into(), min: 0.01, max: 0.3, step: 0.005 },
            ParameterRange { name: "v31_sell_fixed_sl_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_sell_max_hold_lo".into(), min: 10.0, max: 1000.0, step: 10.0 },
            ParameterRange { name: "v31_sell_max_hold_hi".into(), min: 10.0, max: 1000.0, step: 10.0 },
            ParameterRange { name: "v31_sell_max_hold_pow".into(), min: 0.1, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_fee_rate".into(), min: 0.0, max: 0.005, step: 0.0001 },
            ParameterRange { name: "v31_min_hold_bars".into(), min: 1.0, max: 48.0, step: 1.0 },
            ParameterRange { name: "v31_volume_lookback".into(), min: 5.0, max: 100.0, step: 5.0 },
            // 신규 파라미터화된 4개
            ParameterRange { name: "v31_cutoff_tv_mult".into(), min: 0.5, max: 3.0, step: 0.05 },
            ParameterRange { name: "v31_urgent_sell_tv_mult".into(), min: 1.0, max: 5.0, step: 0.1 },
            ParameterRange { name: "v31_sell_ready_price_rise".into(), min: 0.99, max: 1.05, step: 0.002 },
            ParameterRange { name: "v31_sell_wait_max".into(), min: 24.0, max: 720.0, step: 24.0 },
        ]
    }
}
```

- [ ] **Step 2: `strategies/mod.rs`에 등록**

[src-tauri/src/strategies/mod.rs](src-tauri/src/strategies/mod.rs) 상단의 `pub mod` 블록에 추가:

```rust
pub mod enhanced_adaptive;
pub mod regime_adaptive;
pub mod regime_adaptive_v31;  // ← 추가
```

그리고 `StrategyRegistry::new()` 내부의 등록 블록에 추가:

```rust
        registry.register("V3", Box::new(regime_adaptive::RegimeAdaptiveStrategy));
        registry.register("V3.1", Box::new(regime_adaptive_v31::RegimeAdaptiveV31Strategy));  // ← 추가
        registry.register("V5", Box::new(enhanced_adaptive::EnhancedAdaptiveStrategy));
```

- [ ] **Step 3: `cargo build` 로 컴파일 확인**

Run: `cd src-tauri && cargo build --features tauri-app`
Expected: 성공. 이 시점에서 경고로 "v31_* 필드 unused" 가 없어야 함 — 전부 참조됐는지 매치.

- [ ] **Step 4: 커밋**

```bash
git add src-tauri/src/strategies/regime_adaptive_v31.rs src-tauri/src/strategies/mod.rs
git commit -m "feat(v31): implement V3.1 RegimeAdaptive (trade-value based)"
```

---

## Task 3: Optimizer `get_parameter` / `set_parameter` 확장

**Files:**
- Modify: `src-tauri/src/core/optimizer.rs:62-150`

**Goal:** NSGA-II 옵티마이저가 `v31_*` 파라미터들을 읽고 쓸 수 있도록 match 아암 41개 추가. 누락 시 optimizer는 해당 파라미터를 무시하고 기본값으로만 평가.

- [ ] **Step 1: `get_parameter`에 v31 아암 추가**

[src-tauri/src/core/optimizer.rs](src-tauri/src/core/optimizer.rs) 의 `get_parameter` 함수에서 `"v3_volume_lookback"` 아암 다음, `_ => 0.0,` 바로 위에 삽입:

```rust
        "v31_urgent_buy_tv_lo" => params.v31_urgent_buy_tv_lo,
        "v31_urgent_buy_tv_hi" => params.v31_urgent_buy_tv_hi,
        "v31_urgent_buy_tv_pow" => params.v31_urgent_buy_tv_pow,
        "v31_buy_tv_lo" => params.v31_buy_tv_lo,
        "v31_buy_tv_hi" => params.v31_buy_tv_hi,
        "v31_buy_tv_pow" => params.v31_buy_tv_pow,
        "v31_buy_price_drop_lo" => params.v31_buy_price_drop_lo,
        "v31_buy_price_drop_hi" => params.v31_buy_price_drop_hi,
        "v31_buy_price_drop_pow" => params.v31_buy_price_drop_pow,
        "v31_buy_decay_lo" => params.v31_buy_decay_lo,
        "v31_buy_decay_hi" => params.v31_buy_decay_hi,
        "v31_buy_decay_pow" => params.v31_buy_decay_pow,
        "v31_buy_psy_lo" => params.v31_buy_psy_lo,
        "v31_buy_psy_hi" => params.v31_buy_psy_hi,
        "v31_buy_psy_pow" => params.v31_buy_psy_pow,
        "v31_buy_wait_lo" => params.v31_buy_wait_lo,
        "v31_buy_wait_hi" => params.v31_buy_wait_hi,
        "v31_buy_wait_pow" => params.v31_buy_wait_pow,
        "v31_sell_stop_loss_lo" => params.v31_sell_stop_loss_lo,
        "v31_sell_stop_loss_hi" => params.v31_sell_stop_loss_hi,
        "v31_sell_stop_loss_pow" => params.v31_sell_stop_loss_pow,
        "v31_sell_profit_lo" => params.v31_sell_profit_lo,
        "v31_sell_profit_hi" => params.v31_sell_profit_hi,
        "v31_sell_profit_pow" => params.v31_sell_profit_pow,
        "v31_sell_tv_lo" => params.v31_sell_tv_lo,
        "v31_sell_tv_hi" => params.v31_sell_tv_hi,
        "v31_sell_tv_pow" => params.v31_sell_tv_pow,
        "v31_sell_decay_lo" => params.v31_sell_decay_lo,
        "v31_sell_decay_hi" => params.v31_sell_decay_hi,
        "v31_sell_decay_pow" => params.v31_sell_decay_pow,
        "v31_sell_fixed_sl_lo" => params.v31_sell_fixed_sl_lo,
        "v31_sell_fixed_sl_hi" => params.v31_sell_fixed_sl_hi,
        "v31_sell_fixed_sl_pow" => params.v31_sell_fixed_sl_pow,
        "v31_sell_max_hold_lo" => params.v31_sell_max_hold_lo,
        "v31_sell_max_hold_hi" => params.v31_sell_max_hold_hi,
        "v31_sell_max_hold_pow" => params.v31_sell_max_hold_pow,
        "v31_fee_rate" => params.v31_fee_rate,
        "v31_min_hold_bars" => params.v31_min_hold_bars as f64,
        "v31_volume_lookback" => params.v31_volume_lookback as f64,
        "v31_cutoff_tv_mult" => params.v31_cutoff_tv_mult,
        "v31_urgent_sell_tv_mult" => params.v31_urgent_sell_tv_mult,
        "v31_sell_ready_price_rise" => params.v31_sell_ready_price_rise,
        "v31_sell_wait_max" => params.v31_sell_wait_max as f64,
```

- [ ] **Step 2: `set_parameter`에 v31 아암 추가**

동일 파일 `set_parameter` 함수, `"v3_volume_lookback"` 아암 다음, `_ => {}` 위에 삽입:

```rust
        "v31_urgent_buy_tv_lo" => params.v31_urgent_buy_tv_lo = value,
        "v31_urgent_buy_tv_hi" => params.v31_urgent_buy_tv_hi = value,
        "v31_urgent_buy_tv_pow" => params.v31_urgent_buy_tv_pow = value,
        "v31_buy_tv_lo" => params.v31_buy_tv_lo = value,
        "v31_buy_tv_hi" => params.v31_buy_tv_hi = value,
        "v31_buy_tv_pow" => params.v31_buy_tv_pow = value,
        "v31_buy_price_drop_lo" => params.v31_buy_price_drop_lo = value,
        "v31_buy_price_drop_hi" => params.v31_buy_price_drop_hi = value,
        "v31_buy_price_drop_pow" => params.v31_buy_price_drop_pow = value,
        "v31_buy_decay_lo" => params.v31_buy_decay_lo = value,
        "v31_buy_decay_hi" => params.v31_buy_decay_hi = value,
        "v31_buy_decay_pow" => params.v31_buy_decay_pow = value,
        "v31_buy_psy_lo" => params.v31_buy_psy_lo = value,
        "v31_buy_psy_hi" => params.v31_buy_psy_hi = value,
        "v31_buy_psy_pow" => params.v31_buy_psy_pow = value,
        "v31_buy_wait_lo" => params.v31_buy_wait_lo = value,
        "v31_buy_wait_hi" => params.v31_buy_wait_hi = value,
        "v31_buy_wait_pow" => params.v31_buy_wait_pow = value,
        "v31_sell_stop_loss_lo" => params.v31_sell_stop_loss_lo = value,
        "v31_sell_stop_loss_hi" => params.v31_sell_stop_loss_hi = value,
        "v31_sell_stop_loss_pow" => params.v31_sell_stop_loss_pow = value,
        "v31_sell_profit_lo" => params.v31_sell_profit_lo = value,
        "v31_sell_profit_hi" => params.v31_sell_profit_hi = value,
        "v31_sell_profit_pow" => params.v31_sell_profit_pow = value,
        "v31_sell_tv_lo" => params.v31_sell_tv_lo = value,
        "v31_sell_tv_hi" => params.v31_sell_tv_hi = value,
        "v31_sell_tv_pow" => params.v31_sell_tv_pow = value,
        "v31_sell_decay_lo" => params.v31_sell_decay_lo = value,
        "v31_sell_decay_hi" => params.v31_sell_decay_hi = value,
        "v31_sell_decay_pow" => params.v31_sell_decay_pow = value,
        "v31_sell_fixed_sl_lo" => params.v31_sell_fixed_sl_lo = value,
        "v31_sell_fixed_sl_hi" => params.v31_sell_fixed_sl_hi = value,
        "v31_sell_fixed_sl_pow" => params.v31_sell_fixed_sl_pow = value,
        "v31_sell_max_hold_lo" => params.v31_sell_max_hold_lo = value,
        "v31_sell_max_hold_hi" => params.v31_sell_max_hold_hi = value,
        "v31_sell_max_hold_pow" => params.v31_sell_max_hold_pow = value,
        "v31_fee_rate" => params.v31_fee_rate = value,
        "v31_min_hold_bars" => params.v31_min_hold_bars = value as i32,
        "v31_volume_lookback" => params.v31_volume_lookback = value as i32,
        "v31_cutoff_tv_mult" => params.v31_cutoff_tv_mult = value,
        "v31_urgent_sell_tv_mult" => params.v31_urgent_sell_tv_mult = value,
        "v31_sell_ready_price_rise" => params.v31_sell_ready_price_rise = value,
        "v31_sell_wait_max" => params.v31_sell_wait_max = value as i32,
```

- [ ] **Step 3: `cargo build` 컴파일 확인**

Run: `cd src-tauri && cargo build --features tauri-app`
Expected: 성공.

- [ ] **Step 4: 커밋**

```bash
git add src-tauri/src/core/optimizer.rs
git commit -m "feat(v31): wire V3.1 parameters into NSGA-II get/set_parameter"
```

---

## Task 4: V3.1 전용 테스트 작성

**Files:**
- Create: `src-tauri/tests/v31_strategy_test.rs`

**Goal:** V3.1이 (a) registry에 등록되었고, (b) 플랫 데이터에서 충돌 없이 실행되며, (c) 거래대금 스파이크 + 가격 하락 시 buy 신호를 생성함을 검증.

- [ ] **Step 1: 테스트 파일 작성**

Create `src-tauri/tests/v31_strategy_test.rs`:

```rust
use bitcoin_trader_lib::models::market::{Candle, IndicatorSet, MarketData};
use bitcoin_trader_lib::models::trading::TradingParameters;
use bitcoin_trader_lib::strategies::{regime_adaptive_v31::RegimeAdaptiveV31Strategy, Strategy, StrategyRegistry};
use chrono::{Duration, TimeZone, Utc};

fn make_candle(ts_hour: i64, close: f64, volume: f64, rsi: f64, psy: f64) -> MarketData {
    let ts = Utc.with_ymd_and_hms(2026, 1, 1, 0, 0, 0).unwrap() + Duration::hours(ts_hour);
    MarketData {
        candle: Candle {
            timestamp: ts,
            open: close,
            high: close * 1.01,
            low: close * 0.99,
            close,
            volume,
        },
        indicators: IndicatorSet {
            rsi,
            psy_day: psy,
            ..Default::default()
        },
    }
}

#[test]
fn test_v31_registered_in_registry() {
    let reg = StrategyRegistry::new();
    assert!(reg.get("V3.1").is_some(), "V3.1 must be registered");
    assert!(reg.get("V3").is_some(), "V3 must still be registered");
}

#[test]
fn test_v31_parameter_ranges_use_v31_prefix() {
    let s = RegimeAdaptiveV31Strategy;
    let ranges = s.parameter_ranges();
    assert!(!ranges.is_empty());
    for r in &ranges {
        assert!(
            r.name.starts_with("v31_"),
            "parameter {} does not use v31_ prefix",
            r.name
        );
    }
    // 신규 4개 파라미터 노출 검증
    let names: Vec<&str> = ranges.iter().map(|r| r.name.as_str()).collect();
    assert!(names.contains(&"v31_cutoff_tv_mult"));
    assert!(names.contains(&"v31_urgent_sell_tv_mult"));
    assert!(names.contains(&"v31_sell_ready_price_rise"));
    assert!(names.contains(&"v31_sell_wait_max"));
}

#[test]
fn test_v31_flat_data_no_panic() {
    let data: Vec<MarketData> = (0..100)
        .map(|i| make_candle(i as i64, 4_000_000.0, 100.0, 50.0, 0.0))
        .collect();
    let params = TradingParameters::default();
    let s = RegimeAdaptiveV31Strategy;
    let result = s.run_simulation(&data, &params);
    assert_eq!(result.total_trades, 0, "flat data should produce no trades");
}

#[test]
fn test_v31_triggers_on_trade_value_spike() {
    // 시나리오: 100시간 플랫 → 1시간 거래대금 스파이크 + 가격 하락 → buy 기대
    let mut data: Vec<MarketData> = (0..100)
        .map(|i| make_candle(i as i64, 4_000_000.0, 100.0, 50.0, -0.3))
        .collect();

    // spike bar: close 0.93x (drop), volume 1000x → tv 930x — urgent_buy_tv 초과
    data.push(make_candle(100, 3_720_000.0, 100_000.0, 30.0, -0.3));
    // 후속: 반등
    for i in 1..60 {
        data.push(make_candle(100 + i, 4_200_000.0, 200.0, 55.0, 0.1));
    }

    let mut params = TradingParameters::default();
    // 테스트 단순화를 위해 min_hold_bars 짧게
    params.v31_min_hold_bars = 3;

    let s = RegimeAdaptiveV31Strategy;
    let result = s.run_simulation(&data, &params);
    assert!(result.buy_signals > 0, "trade-value spike should generate buy signal");
}
```

- [ ] **Step 2: 테스트 실행**

Run: `cd src-tauri && cargo test --test v31_strategy_test`
Expected: 4 passed, 0 failed.

- [ ] **Step 3: 전체 테스트 실행 (regression 확인)**

Run: `cd src-tauri && cargo test`
Expected: 기존 테스트 모두 여전히 pass. V3/V5 behavior 변화 없음.

- [ ] **Step 4: 커밋**

```bash
git add src-tauri/tests/v31_strategy_test.rs
git commit -m "test(v31): registry, parameter ranges, flat data, tv-spike buy"
```

---

## Task 5: 매뉴얼 작성 + 프론트엔드 영향 확인

**Files:**
- Create: `Manual/strategy-v3.1-trade-value.md`
- Verify: `src/types/index.ts`, `src/pages/OptimizationPage.tsx`

**Goal:** 프로젝트 규칙(CLAUDE.md) 준수: 신규 전략 매뉴얼 작성. 프론트엔드 타입·UI 변경 필요 여부 최종 검증.

- [ ] **Step 1: 매뉴얼 작성**

Create `Manual/strategy-v3.1-trade-value.md`:

```markdown
# V3.1 RegimeAdaptive — Trade Value 기반

## 개요
V3 RegimeAdaptive를 포팅하여 신호 기준을 **거래량(BTC 단위)** → **거래대금(KRW)** 으로 전환한 전략. ETH 전용 트레이딩 환경에서 가격 스케일 변화에 강건.

## V3와의 차이점

### 1. 신호 기준
- **V3**: `volume` (BTC 또는 ETH 수량) 을 urgent/ready/decay 판정에 사용
- **V3.1**: `close × volume` (KRW 거래대금) 을 동일 로직에 사용

### 2. 추가 파라미터 4개 (V3에서 하드코딩이었음)
| 파라미터 | V3 값 | V3.1 범위 | 의미 |
|----------|-------|-----------|------|
| `v31_cutoff_tv_mult` | 1.0 (hard) | 0.5 – 3.0 | 진입 거래대금 대비 몇 배 이상이면 손절 컷오프 |
| `v31_urgent_sell_tv_mult` | 2.0 (hard) | 1.0 – 5.0 | sell_ready_tv 대비 몇 배 이상이면 긴급 매도 |
| `v31_sell_ready_price_rise` | 1.0 (hard) | 0.99 – 1.05 | 매도 준비 가격 상승 비율 |
| `v31_sell_wait_max` | 168 (hard) | 24 – 720 | 매도 확인 대기 최대 바 수 |

## 사용법

### 시뮬레이션
```bash
POST /simulation/run
{
  "strategy_key": "V3.1",
  "market": "KRW-ETH",
  "params": { /* TradingParameters */ }
}
```

### 최적화
`OptimizationPage` 에서 strategy selector에 `V3.1` 선택 — 파라미터 range는 자동으로 `v31_*` 로 노출됨.

## 파라미터 기본값 스케일
ETH/시간 거래대금 기준 (KRW):
- `urgent_buy_tv_lo/hi`: 8.4e10 / 3.0e11
- `buy_tv_lo/hi`: 2.0e10 / 7.0e10
- `sell_tv_lo/hi`: 8.0e9 / 1.26e11

V3의 BTC-volume 기본값에 ETH 평균가 4,000,000 KRW 를 곱한 수준으로 초기화되어 있으나, **실 운용 전 반드시 NSGA-II 재최적화 필수**.

## 주의사항
- V3와 `TradingParameters` struct를 공유함 — V3 파라미터와 혼용 불가, 전략 선택 시 해당 접두어 필드만 참조됨
- `SimulationResult.last_set_volume` 필드는 V3.1에서 "최근 set된 거래대금(KRW)"을 의미 (필드명은 하위호환)
- 트레이스 로깅: `V31_TRACE_PATH` 환경변수로 CSV 출력 (`V3_TRACE_PATH`와 별개)

## 관련 파일
- [src-tauri/src/strategies/regime_adaptive_v31.rs](../src-tauri/src/strategies/regime_adaptive_v31.rs)
- [src-tauri/src/models/trading.rs](../src-tauri/src/models/trading.rs)
- [src-tauri/tests/v31_strategy_test.rs](../src-tauri/tests/v31_strategy_test.rs)
```

- [ ] **Step 2: 프론트엔드 타입 동기화 필요 여부 확인**

Run: `grep -n "v3_\|TradingParameters" src/types/index.ts`
Expected: `TradingParameters` 가 TypeScript에 **field 단위 매핑이 없고**, `Record<string, number>` 또는 `any` 로 처리되고 있다면 수정 불필요. 필드를 명시적으로 enum/interface 한 것이 있다면 v31_* 추가 필요.

- [ ] **Step 3: `npm run vite:build` 로 프론트엔드 타입 체크**

Run: `cd d:/SW/bitcoin-trader && npm run vite:build`
Expected: 성공 (타입 에러 없음). 전략 key `"V3.1"` 이 프론트엔드에 하드코딩되어 있지 않아야 — OptimizationPage가 registry의 list()를 동적으로 받아온다면 자동 노출됨.

- [ ] **Step 4: `History.md` 업데이트**

`History.md` 상단에 다음 항목 추가:

```markdown
## 2026-04-23
- 16:XX success: V3.1 RegimeAdaptive (trade-value) 전략 추가 — V3 포팅, 거래대금 기준 신호, 하드코딩 상수 4개 파라미터화
```

- [ ] **Step 5: 최종 전체 검증**

Run (순차 실행):
```bash
cd d:/SW/bitcoin-trader/src-tauri && cargo build --features tauri-app
cd d:/SW/bitcoin-trader/src-tauri && cargo test
cd d:/SW/bitcoin-trader && npm run vite:build
```

Expected: 3개 모두 성공.

- [ ] **Step 6: 커밋**

```bash
git add Manual/strategy-v3.1-trade-value.md History.md
git commit -m "docs(v31): add strategy manual and history entry"
```

---

## Self-Review Checklist

- [x] **Spec coverage:** V3 포팅 ✓, 거래대금 전환 ✓, 4개 상수 파라미터화 ✓, ETH 스케일 기본값 ✓, 매뉴얼 ✓
- [x] **No placeholders:** 모든 코드 블록이 완전한 실행 가능 코드. TBD/TODO 없음
- [x] **Type consistency:** `v31_*` 필드 이름이 trading.rs, regime_adaptive_v31.rs, optimizer.rs, 테스트 전반에서 일치. `set_trade_value` (로컬) vs `last_set_volume` (struct 공용) 의도적 분리 — 주석 표시됨
- [x] **파일 경로:** 모두 절대/상대 경로 명시
- [x] **빌드 확인 단계:** 각 Task 후 `cargo build`, Task 4에서 전체 `cargo test`, Task 5에서 `npm run vite:build`

---

## Risk & Mitigation

| 리스크 | 완화책 |
|--------|--------|
| V3/V3.1 간 파라미터 struct 공유로 필드 ~70개 → `TradingParameters` 비대화 | V6+ 예정 전략도 동일 패턴 따르면 struct 분할(trait) 검토. 지금은 과도한 추상화 지양 |
| ETH 거래대금 기본값이 실 운용에 안 맞음 | 매뉴얼에 "NSGA-II 재최적화 필수" 명시. 테스트에서 default 값 사용 안 함 |
| 기존 DB에 저장된 파라미터 JSON에 v31_* 필드 없음 | `#[serde(default = "d_v31_*")]` 로 역직렬화 호환성 확보 |
| 프론트엔드 OptimizationPage가 전략별 파라미터 목록을 하드코딩 | 현재 registry.list() 기반이므로 자동 노출. Step 2에서 검증 |
